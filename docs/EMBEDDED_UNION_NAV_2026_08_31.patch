diff --git a/core/src/main/java/com/legend/normalizer/UnionSynthesis.java b/core/src/main/java/com/legend/normalizer/UnionSynthesis.java
index 1f4bb797..75e6b35c 100644
--- a/core/src/main/java/com/legend/normalizer/UnionSynthesis.java
+++ b/core/src/main/java/com/legend/normalizer/UnionSynthesis.java
@@ -1268,7 +1268,8 @@ final class UnionSynthesis {
         }
         for (String top : embTops) {
             ctor.put(top, new KeyExpression(
-                    rebuildEmbCtor(top, embSubs, embInner, row, model),
+                    rebuildEmbCtor(top, embSubs, embInner, emb.navSubs(),
+                            row, model),
                     false, false));
         }
         return new AppliedFunction("map", List.of(union,
@@ -1370,7 +1371,8 @@ final class UnionSynthesis {
      * top prop with ANY unprojectable leaf (join-slot sub-read) poisons
      * WHOLE — conservative, never a silently-wrong projection. */
     private record EmbDist(Map<String, LinkedHashSet<String>> subs,
-            Map<String, String> inner, LinkedHashSet<String> tops) {
+            Map<String, String> inner, LinkedHashSet<String> tops,
+            Map<String, LinkedHashSet<String>> navSubs) {
     }
 
     private static EmbDist collectEmbeddedDistribution(
@@ -1379,6 +1381,7 @@ final class UnionSynthesis {
         Map<String, LinkedHashSet<String>> embSubs = new LinkedHashMap<>();
         Map<String, String> embInner = new LinkedHashMap<>();
         Map<String, LinkedHashSet<String>> pathClasses = new LinkedHashMap<>();
+        Map<String, LinkedHashSet<String>> navSubs = new LinkedHashMap<>();
         Set<String> poisoned = new LinkedHashSet<>();
         for (MappingNormalizer.RelationalParts pp : parts) {
             for (var fe : pp.fields().entrySet()) {
@@ -1395,7 +1398,7 @@ final class UnionSynthesis {
                 if (ni != null) {
                     collectEmbLeaves(fe.getKey(), fe.getKey(), ni,
                             pp.rowBind().name(), embSubs, embInner, poisoned,
-                            pathClasses);
+                            pathClasses, navSubs, model);
                 }
             }
         }
@@ -1432,6 +1435,14 @@ final class UnionSynthesis {
                     embSubs.remove(path);
                 }
             }
+            LinkedHashSet<String> nv = navSubs.get(path);
+            if (nv != null) {
+                nv.removeIf(leaf -> MappingNormalizer
+                        .findPropertyTypeDeep(decl0, leaf, model) == null);
+                if (nv.isEmpty()) {
+                    navSubs.remove(path);
+                }
+            }
             // NESTED ctor subtrees under a prop the declared class does
             // not carry (Airline's planes under VehicleOwner) prune too —
             // the recompose loop would re-enter them as ctor fields
@@ -1442,18 +1453,24 @@ final class UnionSynthesis {
                             model) == null;
             embInner.keySet().removeIf(off);
             embSubs.keySet().removeIf(off);
+            navSubs.keySet().removeIf(off);
         }
         for (String bad : poisoned) {
             embSubs.keySet().removeIf(k -> k.equals(bad)
                     || k.startsWith(bad + "."));
             embInner.keySet().removeIf(k -> k.equals(bad)
                     || k.startsWith(bad + "."));
+            navSubs.keySet().removeIf(k -> k.equals(bad)
+                    || k.startsWith(bad + "."));
         }
         LinkedHashSet<String> tops = new LinkedHashSet<>();
         for (String k : embSubs.keySet()) {
             tops.add(k.contains(".") ? k.substring(0, k.indexOf('.')) : k);
         }
-        return new EmbDist(embSubs, embInner, tops);
+        for (String k : navSubs.keySet()) {
+            tops.add(k.contains(".") ? k.substring(0, k.indexOf('.')) : k);
+        }
+        return new EmbDist(embSubs, embInner, tops, navSubs);
     }
 
     /** One member thread's embedded sub-columns (schema-contract handling
@@ -1502,7 +1519,8 @@ final class UnionSynthesis {
             NewInstance ni, String rowVar,
             Map<String, LinkedHashSet<String>> embSubs,
             Map<String, String> embInner, Set<String> poisoned,
-            Map<String, LinkedHashSet<String>> pathClasses) {
+            Map<String, LinkedHashSet<String>> pathClasses,
+            Map<String, LinkedHashSet<String>> navSubs, ModelBuilder model) {
         embInner.putIfAbsent(pathKey, ni.className());
         pathClasses.computeIfAbsent(pathKey, k -> new LinkedHashSet<>())
                 .add(ni.className());
@@ -1510,8 +1528,33 @@ final class UnionSynthesis {
             NewInstance sub = ctorOf(pe.expression().value());
             if (sub != null) {
                 collectEmbLeaves(top, pathKey + "." + pe.key(), sub,
-                        rowVar, embSubs, embInner, poisoned, pathClasses);
-            } else if (isThreadProjectable(pe.expression().value(), rowVar)) {
+                        rowVar, embSubs, embInner, poisoned, pathClasses,
+                        navSubs, model);
+                continue;
+            }
+            // CLASS-TYPED sub (an embedded ctor field holding a nav
+            // read, $row.employees): NEVER a member thread column — a
+            // class collection has no scalar projection. It recomposes
+            // as a plain UNION-LEVEL read served by the nav lift
+            // (collectNavLifts' embedded descent — one navigation
+            // mechanism, no member-level twin). Resolver-bug burn
+            // 2026-08-31, the embedded-union witnesses.
+            TypeExpression st = MappingNormalizer.findPropertyTypeDeep(
+                    MissProbe.knownMiss(model.findClass(ni.className())),
+                    pe.key(), model);
+            boolean sameNameRead = pe.expression().value()
+                    instanceof AppliedProperty ap0
+                    && ap0.receiver()
+                            instanceof com.legend.protocol.spec.Variable rv0
+                    && rv0.name().equals(rowVar)
+                    && ap0.property().equals(pe.key());
+            if (false && sameNameRead && st instanceof TypeExpression.NameRef snr   // PROBE2
+                    && model.findClass(snr.name()).isPresent()) {
+                navSubs.computeIfAbsent(pathKey,
+                        k -> new LinkedHashSet<>()).add(pe.key());
+                continue;
+            }
+            if (isThreadProjectable(pe.expression().value(), rowVar)) {
                 embSubs.computeIfAbsent(pathKey, k -> new LinkedHashSet<>())
                         .add(pe.key());
             } else {
@@ -1541,16 +1584,25 @@ final class UnionSynthesis {
     /** Recompose the union root's embedded ctor from projected columns. */
     private static ValueSpecification rebuildEmbCtor(String path,
             Map<String, LinkedHashSet<String>> embSubs,
-            Map<String, String> embInner, Variable row, ModelBuilder model) {
+            Map<String, String> embInner,
+            Map<String, LinkedHashSet<String>> navSubs, Variable row,
+            ModelBuilder model) {
         Map<String, KeyExpression> fields = new LinkedHashMap<>();
         for (String sub : embSubs.getOrDefault(path, new LinkedHashSet<>())) {
             fields.put(sub, new KeyExpression(
                     new AppliedProperty(row, embCol(path, sub)), false, false));
         }
+        // class-typed subs recompose as PLAIN union-level reads — the
+        // lifted navigation serves them (collectEmbLeaves' nav arm)
+        for (String sub : navSubs.getOrDefault(path, new LinkedHashSet<>())) {
+            fields.put(sub, new KeyExpression(
+                    new AppliedProperty(row, sub), false, false));
+        }
         for (String k : embInner.keySet()) {
             if (k.startsWith(path + ".") && k.indexOf('.', path.length() + 1) < 0) {
                 fields.put(k.substring(path.length() + 1), new KeyExpression(
-                        rebuildEmbCtor(k, embSubs, embInner, row, model),
+                        rebuildEmbCtor(k, embSubs, embInner, navSubs, row,
+                                model),
                         false, false));
             }
         }
@@ -1930,43 +1982,104 @@ final class UnionSynthesis {
         return true;
     }
 
+    /** One member's Join-PM scan (top level + EMBEDDED descent): each
+     * class-typed Join records (ordinal, join, TARGET CLASS) under its
+     * property name — the target derives from the property's OWNING
+     * class exactly once, at scan. {@code declaredOwner} walks the
+     * UNION-DECLARED chain in parallel: a property the declared chain
+     * does not carry (subtype-only PMs — Street.coordinate under an
+     * Address union) is NOT lifted; it stays member-local for the stc
+     * subtype dispatch (this gate previously hid inside the lift
+     * loop's union-class re-derivation). A name colliding across
+     * DIFFERENT target classes is LOUD (merging such entries would OR
+     * unrelated navigations). */
+    private static void scanJoinPms(List<PropertyMapping> pms,
+            @com.legend.Nullable ClassDefinition owner,
+            @com.legend.Nullable ClassDefinition declaredOwner, int ordinal,
+            LegacyMappingDefinition md, String unionClassName,
+            Map<String, List<int[]>> found,
+            Map<String, List<PropertyMapping.Join>> joins,
+            Map<String, String> targetByProp, ModelBuilder model) {
+        for (PropertyMapping pm : pms) {
+            if (false && pm instanceof PropertyMapping.Embedded e) {   // PROBE
+                TypeExpression et = owner == null ? null
+                        : MappingNormalizer.findPropertyTypeDeep(owner,
+                                e.propertyName(), model);
+                ClassDefinition subOwner = et instanceof TypeExpression.NameRef nr
+                        ? model.findClass(nr.name()).orElse(null) : null;
+                TypeExpression dt = declaredOwner == null ? null
+                        : MappingNormalizer.findPropertyTypeDeep(
+                                declaredOwner, e.propertyName(), model);
+                ClassDefinition subDeclared = dt instanceof TypeExpression.NameRef dnr
+                        ? model.findClass(dnr.name()).orElse(null) : null;
+                scanJoinPms(e.propertyMappings(), subOwner, subDeclared,
+                        ordinal, md, unionClassName, found, joins,
+                        targetByProp, model);
+                continue;
+            }
+            if (!(pm instanceof PropertyMapping.Join j)) {
+                continue;
+            }
+            if (declaredOwner == null || MappingNormalizer
+                    .findPropertyTypeDeep(declaredOwner,
+                            pm.propertyName(), model) == null) {
+                continue;   // subtype-only: stc dispatch owns it
+            }
+            TypeExpression pt = owner == null ? null
+                    : MappingNormalizer.findPropertyTypeDeep(owner,
+                            pm.propertyName(), model);
+            if (!(pt instanceof TypeExpression.NameRef pnr)
+                    || model.findClass(pnr.name()).isEmpty()) {
+                continue;   // scalar join-terminal shapes stay member-local
+            }
+            String prior = targetByProp.putIfAbsent(pm.propertyName(),
+                    pnr.name());
+            if (prior != null && !prior.equals(pnr.name())) {
+                throw new ModelException(LegendCompileException.Phase.NORMALIZE,
+                        "union nav lift name collision: property '"
+                        + pm.propertyName() + "' navigates to '" + prior
+                        + "' and '" + pnr.name() + "' across members of '"
+                        + unionClassName + "' (mapping "
+                        + md.qualifiedName() + ")");
+            }
+            found.computeIfAbsent(pm.propertyName(), k -> new ArrayList<>())
+                    .add(new int[]{ordinal});
+            joins.computeIfAbsent(pm.propertyName(), k -> new ArrayList<>())
+                    .add(j);
+        }
+    }
+
     static List<NavLift> collectNavLifts(LegacyMappingDefinition md,
             String className, List<ClassMapping> members,
             ModelBuilder model) {
-        // property -> per-member entries, member order
+        // property -> per-member entries, member order. The TARGET class
+        // records AT SCAN TIME from the property's OWNING class (one
+        // derivation; the lift loop's former union-class re-derivation —
+        // a second owner of the same fact — is deleted). EMBEDDED
+        // sub-PMs descend: a class-typed Join inside an embedded ctor is
+        // a union-level navigation like any other (its recomposed ctor
+        // keeps the $row.<sub> read that this lift serves — the
+        // embedded-union witnesses' formerly-unservable read).
         Map<String, List<int[]>> found = new LinkedHashMap<>();
         Map<String, List<PropertyMapping.Join>> joins = new LinkedHashMap<>();
+        Map<String, String> targetByProp = new LinkedHashMap<>();
         for (int i = 0; i < members.size(); i++) {
             if (!(members.get(i) instanceof ClassMapping.Relational mr)) {
                 continue;   // Relation(~func) members carry no Join PMs
             }
             ClassDefinition memberOwner = model.findClass(mr.className()).orElseThrow(() -> new IllegalStateException("F7.8: class unresolved at UnionSynthesis#13 (this default NEVER fired on the corpus census; a miss here is a real model gap): " + mr.className()));
-            for (PropertyMapping pm : mr.propertyMappings()) {
-                if (!(pm instanceof PropertyMapping.Join j)) {
-                    continue;
-                }
-                TypeExpression pt = memberOwner == null ? null
-                        : MappingNormalizer.findPropertyTypeDeep(memberOwner, pm.propertyName(), model);
-                if (!(pt instanceof TypeExpression.NameRef pnr)
-                        || model.findClass(pnr.name()).isEmpty()) {
-                    continue;   // scalar join-terminal shapes stay member-local
-                }
-                found.computeIfAbsent(pm.propertyName(), k -> new ArrayList<>())
-                        .add(new int[]{i});
-                joins.computeIfAbsent(pm.propertyName(), k -> new ArrayList<>())
-                        .add(j);
-            }
+            scanJoinPms(mr.propertyMappings(), memberOwner,
+                    model.findClass(className).orElse(null), i, md,
+                    className, found, joins, targetByProp, model);
         }
         List<NavLift> lifts = new ArrayList<>();
         for (String prop : found.keySet()) {
-            ClassDefinition owner = model.findClass(className).orElseThrow(() -> new IllegalStateException("F7.8: class unresolved at UnionSynthesis#14 (this default NEVER fired on the corpus census; a miss here is a real model gap): " + className));
-            TypeExpression pt = owner == null ? null
-                    : MappingNormalizer.findPropertyTypeDeep(owner, prop, model);
-            if (!(pt instanceof TypeExpression.NameRef pnr)
-                    || !model.isMappedClass(pnr.name())) {
+            String targetClassFqn = java.util.Objects.requireNonNull(
+                    targetByProp.get(prop),
+                    "scan recorded a Join PM without its target class");
+            if (!model.isMappedClass(targetClassFqn)) {
                 continue;
             }
-            String targetClassFqn = pnr.name();
             // BITEMPORAL UNGATE (Leg 2): the per-dimension stampers
             // (milestonedPipeByStrategy walks only tables CARRYING each
             // dimension) are capability-aware by construction after the
diff --git a/core/src/main/java/com/legend/resolver/Pipelines.java b/core/src/main/java/com/legend/resolver/Pipelines.java
index c535f23f..d2f618d8 100644
--- a/core/src/main/java/com/legend/resolver/Pipelines.java
+++ b/core/src/main/java/com/legend/resolver/Pipelines.java
@@ -139,6 +139,18 @@ public final class Pipelines {
     /** Resolves a navigate step's target class to its (slot-stripped) pipeline. */
     interface TargetResolver {
         TypedSpec pipelineFor(String alias, String targetClassFqn);
+
+        /** Column-narrowed resolve (resolver-bug burn 2026-08-31):
+         * {@code needed} = the columns the CONSUMER reads off this
+         * target (condition keys + colspec read tails; null = all).
+         * A narrowing resolver drops unread member columns BEFORE
+         * materializing — which stops unread nav-reading columns from
+         * demanding their own targets (the Firm↔Person embedded-union
+         * cycle). Default ignores the hint. */
+        default TypedSpec pipelineFor(String alias, String targetClassFqn,
+                @com.legend.Nullable Set<String> needed) {
+            return pipelineFor(alias, targetClassFqn);
+        }
     }
 
     /** All navigate-step aliases in {@code pipeline} (class-typed Join PMs). */
@@ -413,6 +425,34 @@ public final class Pipelines {
             withFilters.addAll(filterSlots);
             demanded = closeOverConditions(pipeline, withFilters);
         }
+        // (An unconditional closeOverConditions here was BUILT AND
+        // REVERTED 2026-08-31: pulling sibling slots into demand
+        // through UNDEMANDED chained conditions tripped the deep-
+        // composite wall on innerjoin isolation — the caller-side
+        // closures encode per-route judgment, not forgetfulness.)
+        // NAV-TARGET COLUMN NEEDS (same burn): what each nav target's
+        // CONSUMERS read — its predicate's target-side keys plus every
+        // colspec's read tails through the alias (null = a whole-object
+        // read somewhere, or unknown → full). A narrowing resolver uses
+        // this to drop unread member columns BEFORE materializing.
+        final TargetResolver inner = targets;
+        if (inner != null && !navs.isEmpty()) {
+            Map<String, Set<String>> needs = new LinkedHashMap<>();
+            Set<String> whole = new LinkedHashSet<>();
+            collectNavNeeds(pipeline, navs.keySet(), needs, whole);
+            for (var e : navs.entrySet()) {
+                if (e.getValue().predicate().parameters().size() == 2) {
+                    for (TypedSpec b : e.getValue().predicate().body()) {
+                        collectVarReads(b,
+                                e.getValue().predicate().parameters().get(1),
+                                needs.computeIfAbsent(e.getKey(),
+                                        k -> new LinkedHashSet<>()));
+                    }
+                }
+            }
+            targets = (alias, fqn) -> inner.pipelineFor(alias, fqn,
+                    whole.contains(alias) ? null : needs.get(alias));
+        }
         if (!filterNavs.isEmpty() && targets != null) {
             Set<String> withFilters = new LinkedHashSet<>(demandedNavs);
             withFilters.addAll(filterNavs);
@@ -560,8 +600,12 @@ public final class Pipelines {
                     yield left;   // CLASS-SLOT JOIN CANCELLED
                 }
                 if (targets == null) {
-                    throw new IllegalStateException(
+                    IllegalStateException e = new IllegalStateException(
                             "resolver bug: demanded navigate step without a target resolver");
+                    if (System.getenv("LL_TMP_DEBUG") != null) {
+                        e.printStackTrace();
+                    }
+                    throw e;
                 }
                 if (!(nav.target() instanceof TypedGetAll ga)) {
                     throw new IllegalStateException("resolver bug: navigate step '"
@@ -662,7 +706,14 @@ public final class Pipelines {
             case TypedProject pr
                     when containsSlot(pr.source()) || !navSteps(pr.source()).isEmpty() -> {
                 Set<String> slotAliases = slotAliases(pr.source());
+                // BOTH demand vocabularies (resolver-bug burn
+                // 2026-08-31): colspec bodies read JOIN SLOTS and
+                // NAVIGATE STEPS — the arm collected only slots and
+                // hard-passed empty nav demand, so an embedded-union
+                // branch's nav read walled on its own stripped step
+                // ('employees', the union-embedded witnesses).
                 Set<String> ownDemand = new LinkedHashSet<>();
+                Set<String> ownNavDemand = new LinkedHashSet<>();
                 for (var col : pr.columns()) {
                     String rv = col.fn().parameters().get(0);
                     for (TypedSpec b : col.fn().body()) {
@@ -684,8 +735,8 @@ public final class Pipelines {
                 ownDemand = closeOverConditions(pr.source(), ownDemand);
                 Map<String, String> branchPrefixes = new LinkedHashMap<>();
                 Set<String> branchStripped = new LinkedHashSet<>();
-                TypedSpec src = walk(pr.source(), ownDemand, Set.of(), targets,
-                        branchPrefixes, branchStripped, classFqn);
+                TypedSpec src = walk(pr.source(), ownDemand, ownNavDemand,
+                        targets, branchPrefixes, branchStripped, classFqn);
                 List<TypedFuncCol> cols =
                         new ArrayList<>(pr.columns().size());
                 for (var col : pr.columns()) {
@@ -1197,6 +1248,87 @@ public final class Pipelines {
     }
 
     /** Slot aliases read through {@code rowVar} in {@code n} ($row.slot...). */
+    /** Per-alias read TAILS through nav steps in every colspec body:
+     * {@code $rv.alias.tail} contributes {@code tail}; a bare
+     * {@code $rv.alias} (whole-object) marks the alias FULL. */
+    private static void collectNavNeeds(TypedSpec n, Set<String> navAliases,
+            Map<String, Set<String>> needs, Set<String> whole) {
+        if (n instanceof TypedProject pr) {
+            for (var col : pr.columns()) {
+                String rv = col.fn().parameters().get(0);
+                for (TypedSpec b : col.fn().body()) {
+                    collectNavTails(b, rv, navAliases, needs, whole);
+                }
+            }
+        }
+        for (TypedSpec c : n.children()) {
+            collectNavNeeds(c, navAliases, needs, whole);
+        }
+    }
+
+    private static void collectNavTails(TypedSpec n, String rowVar,
+            Set<String> navAliases, Map<String, Set<String>> needs,
+            Set<String> whole) {
+        if (n instanceof TypedPropertyAccess outer
+                && outer.source() instanceof TypedPropertyAccess pa
+                && pa.source() instanceof TypedVariable v
+                && v.name().equals(rowVar)
+                && navAliases.contains(pa.property())) {
+            needs.computeIfAbsent(pa.property(),
+                    k -> new LinkedHashSet<>()).add(outer.property());
+            return;
+        }
+        if (n instanceof TypedPropertyAccess pa
+                && pa.source() instanceof TypedVariable v
+                && v.name().equals(rowVar)
+                && navAliases.contains(pa.property())) {
+            whole.add(pa.property());
+            return;
+        }
+        if (n instanceof TypedLambda l && l.parameters().contains(rowVar)) {
+            return;     // shadowing lambda: its $rowVar is NOT our row
+        }
+        for (TypedSpec c : n.children()) {
+            collectNavTails(c, rowVar, navAliases, needs, whole);
+        }
+    }
+
+    /** Member-projection NARROWING (the burn's cycle cut): keep only
+     * {@code needed} columns in the pipeline's top projections
+     * (descending through filters and concatenate branches — all
+     * branches narrow by the SAME set, preserving union arity).
+     * Unknown shapes and empty intersections narrow nothing. */
+    static TypedSpec narrowMembers(TypedSpec p, Set<String> needed) {
+        if (p instanceof TypedFilter f) {
+            TypedSpec s = narrowMembers(f.source(), needed);
+            return s == f.source() ? p : new TypedFilter(s, f.predicate(),
+                    new ExprType(s.info().type(), Multiplicity.Bounded.ONE));
+        }
+        if (p instanceof TypedConcatenate c) {
+            TypedSpec l = narrowMembers(c.left(), needed);
+            TypedSpec r = narrowMembers(c.right(), needed);
+            return l == c.left() && r == c.right() ? p
+                    : new TypedConcatenate(l, r,
+                            new ExprType(l.info().type(),
+                                    Multiplicity.Bounded.ONE));
+        }
+        if (p instanceof TypedProject pr) {
+            List<TypedFuncCol> keep = pr.columns().stream()
+                    .filter(col -> needed.contains(col.name())).toList();
+            if (keep.isEmpty() || keep.size() == pr.columns().size()) {
+                return p;
+            }
+            Type.RelationType row =
+                    Type.requireRelationSchema(pr.info().type());
+            List<Type.Column> cols = row.columns().stream()
+                    .filter(c -> needed.contains(c.name())).toList();
+            return new TypedProject(pr.source(), keep,
+                    new ExprType(Type.relation(new Type.RelationType(cols)),
+                            Multiplicity.Bounded.ONE));
+        }
+        return p;
+    }
+
     private static void collectSlotReads(TypedSpec n, String rowVar,
             Set<String> slotAliases, Set<String> out) {
         if (n instanceof TypedPropertyAccess pa
@@ -1244,14 +1376,24 @@ public final class Pipelines {
                 && pa.source() instanceof TypedVariable v
                 && v.name().equals(rowVar)) {
             if (prefixes.containsKey(pa.property())) {
-                throw new IllegalStateException("resolver bug: converted-slot read"
+                IllegalStateException e0 = new IllegalStateException(
+                        "resolver bug: converted-slot read"
                         + " in unrecognized shape — $" + rowVar + "." + pa.property()
                         + " consumed other than as a sub-row column read");
+                if (System.getenv("LL_TMP_DEBUG") != null) {
+                    e0.printStackTrace();
+                }
+                throw e0;
             }
             if (stripped.contains(pa.property())) {
-                throw new IllegalStateException("resolver bug: undemanded navigation —"
+                IllegalStateException e = new IllegalStateException(
+                        "resolver bug: undemanded navigation —"
                         + " consumed expression reads STRIPPED join slot '"
                         + pa.property() + "' (the demand scan and the rewrite disagreed)");
+                if (System.getenv("LL_TMP_DEBUG") != null) {
+                    e.printStackTrace();
+                }
+                throw e;
             }
         }
         return switch (n) {
diff --git a/core/src/main/java/com/legend/resolver/StoreResolver.java b/core/src/main/java/com/legend/resolver/StoreResolver.java
index cee9f19c..ecc615b5 100644
--- a/core/src/main/java/com/legend/resolver/StoreResolver.java
+++ b/core/src/main/java/com/legend/resolver/StoreResolver.java
@@ -1292,7 +1292,8 @@ public final class StoreResolver {
 
 
     private NavPlan registerNavigations(ClassSource cs,
-            Set<List<String>> paths, Set<String> splitChains) {
+            Set<List<String>> paths, Set<String> splitChains,
+            Set<List<String>> filterPaths) {
         // Slot demand (heads whose bindings read join slots).
         Set<String> slotAliases = Pipelines.slotAliases(cs.pipeline());
         Set<String> demanded = new LinkedHashSet<>();
@@ -1361,8 +1362,23 @@ public final class StoreResolver {
                         && com.legend.builtin.Pure.isToOneCall(tc1.callee().qualifiedName())) {
                     inner = tc1.args().get(0);
                 }
+                // drill INCLUDES the leaf FOR FILTER PATHS ONLY
+                // (resolver-bug burn 2026-08-31): a filter 2-hop path's
+                // ctor FIELD can itself be the nav read
+                // ($f.bridge.employees->exists — the embedded-union
+                // recompose); the old mid-only bound left the leaf
+                // undrilled and the lifted nav undemanded. PROJECTION
+                // paths (graphFetch tree nodes) keep the mid-only bound
+                // — a root-level leaf join would FAN the rows (the
+                // embedded graphFetch 3→5 regression). Non-ctor fields
+                // end the loop, so deeper paths are unaffected.
+                // ... and only over UNION pipelines: the leaf's serving
+                // nav is the union-level LIFT; non-union embedded
+                // exists paths keep their per-leaf otherwise/partial
+                // route (innerjoin isolation regression receipt)
+                int drillBound = path.size() - 1;   // PROBE3: drill off
                 if (inner instanceof TypedNewInstance ni
-                        && mid + 1 < path.size()
+                        && mid < drillBound
                         && ni.properties().containsKey(
                                 SyntheticHeads.realHead(path.get(mid)))) {
                     // a SYNTHETIC mid component drills by its REAL property;
@@ -1786,13 +1802,54 @@ public final class StoreResolver {
                         + " silently drop the correlation");
             }
         }
+        // the fallback target resolve threads ITSELF (resolver-bug burn
+        // 2026-08-31): a nav target's own pipeline may carry nav-reading
+        // member projections (embedded-union members) whose demand needs
+        // the resolver recursively — a null hand-off walled them. The
+        // NARROWING overload drops unread member columns first (their
+        // navs never demand — the Firm↔Person cycle's natural cut); a
+        // genuine full-width cycle stays LOUD, never a hang.
+        Set<String> inFlight = new LinkedHashSet<>();
+        Pipelines.TargetResolver self = new Pipelines.TargetResolver() {
+            @Override
+            public TypedSpec pipelineFor(String alias, String targetClass) {
+                return pipelineFor(alias, targetClass, null);
+            }
+
+            @Override
+            public TypedSpec pipelineFor(String alias, String targetClass,
+                    @com.legend.Nullable Set<String> needed) {
+                if (navMats.containsKey(alias)) {
+                    return navMats.get(alias).pipeline();
+                }
+                // FIRST-LEVEL resolves keep FULL WIDTH (substitution
+                // reads prefixed columns the colspec-computed `needed`
+                // cannot see — narrowing here dropped them); RECURSIVE
+                // resolves narrow to the consumer's reads, which is
+                // exactly the Firm↔Person embedded-union cycle cut.
+                boolean recursive = !inFlight.isEmpty();
+                if (!inFlight.add(targetClass)) {
+                    throw new IllegalStateException("resolver bug: nav"
+                            + " target cycle through '" + targetClass
+                            + "' at full width — a consumer demands a"
+                            + " nav-reading column of a class already"
+                            + " being resolved");
+                }
+                try {
+                    TypedSpec pipe = sources
+                            .get(cs.mappingFqn(), targetClass).pipeline();
+                    if (recursive && needed != null) {
+                        pipe = Pipelines.narrowMembers(pipe, needed);
+                    }
+                    return Pipelines.materialize(pipe, Set.of(), Set.of(),
+                            targetClass, this).pipeline();
+                } finally {
+                    inFlight.remove(targetClass);
+                }
+            }
+        };
         Pipelines.Materialized m = Pipelines.materialize(
-                csPipe, demanded, demandedNavs, cs.classFqn(),
-                (alias, targetClass) -> navMats.containsKey(alias)
-                        ? navMats.get(alias).pipeline()
-                        : Pipelines.materialize(
-                                sources.get(cs.mappingFqn(), targetClass).pipeline(),
-                                Set.of(), targetClass).pipeline());
+                csPipe, demanded, demandedNavs, cs.classFqn(), self);
         // §4AD P1 placement bit, slot channel (Pipelines owns the rule)
         m = Pipelines.innerizeValueSlots(m, navHeadByAlias, synthetics);
         Map<String, String> navPrefixToClass = new LinkedHashMap<>();
@@ -2141,7 +2198,34 @@ public final class StoreResolver {
             if (cs.bindings().containsKey(SyntheticHeads.realHead(head))) {
                 // A NAVIGATE-SLOT head (class-typed Join PM): the nav step
                 // carries the target extent + oriented (s, t) predicate.
-                var nav = Pipelines.navSteps(cs.pipeline()).get(SyntheticHeads.realHead(head));
+                String navAlias = SyntheticHeads.realHead(head);
+                String registerKey = head;
+                TypedSpec hb1 = cs.bindings().get(navAlias);
+                if (hb1 instanceof TypedNativeCall toc
+                        && toc.args().size() == 1
+                        && com.legend.builtin.Pure.isToOneCall(
+                                toc.callee().qualifiedName())) {
+                    hb1 = toc.args().get(0);
+                }
+                if (filterTwoHop
+                        && Pipelines.navSteps(cs.pipeline()).get(navAlias) == null
+                        && hb1 instanceof TypedNewInstance ctor0
+                        && ctor0.properties().get(SyntheticHeads
+                                .realHead(path.get(1)))
+                                instanceof TypedPropertyAccess cf
+                        && cf.source() instanceof TypedVariable cfv
+                        && cfv.name().equals(cs.rowVar())
+                        && Pipelines.navSteps(cs.pipeline())
+                                .containsKey(cf.property())) {
+                    // EMBEDDED head (resolver-bug burn 2026-08-31): the
+                    // ctor's LEAF field is the nav read — the exists
+                    // material registers under the DOTTED consumer path
+                    // (registerNavigations' headKey convention), serving
+                    // $f.bridge.employees->exists(...) like a direct nav.
+                    navAlias = cf.property();
+                    registerKey = String.join(".", path);
+                }
+                var nav = Pipelines.navSteps(cs.pipeline()).get(navAlias);
                 if (nav == null || !(nav.target()
                         instanceof TypedGetAll tg)
                         // eager material only when the target class IS
@@ -2151,12 +2235,18 @@ public final class StoreResolver {
                     continue;
                 }
                 ClassSource t = sources.getForNav(cs.mappingFqn(),
-                        tg.classFqn(), head);
+                        tg.classFqn(), registerKey);
                 Set<String> tSlots0 = Pipelines.slotAliases(t.pipeline());
                 Set<String> tDemand0 = new LinkedHashSet<>();
-                Set<String> innerLeaves = new LinkedHashSet<>(
-                        InnerDemand.leaves(ops, head));
-                for (TypedLambda liftedPred0 : synthetics.allPreds(head)) {
+                Set<String> innerLeaves = new LinkedHashSet<>();
+                for (TypedLambda lam : InnerDemand.lambdas(ops,
+                        registerKey.equals(head) ? List.of(head) : path)) {
+                    if (!lam.parameters().isEmpty()) {
+                        InnerDemand.collectParamPathHeads(lam,
+                                lam.parameters().get(0), innerLeaves);
+                    }
+                }
+                for (TypedLambda liftedPred0 : synthetics.allPreds(registerKey)) {
                     for (TypedSpec b : liftedPred0.body()) {
                         InnerDemand.collectParamPathHeads(b,
                                 liftedPred0.parameters().get(0), innerLeaves);
@@ -2175,12 +2265,12 @@ public final class StoreResolver {
                 // materialization join them, and build depth-1 SubNavs for
                 // the composition's pass-1 dispatch. Deeper hops stay loud
                 // (empty children).
-                TypedLambda corrNav0 = synthetics.correlatedPred(head);
+                TypedLambda corrNav0 = synthetics.correlatedPred(registerKey);
                 Map<String, String> predNavAliases = new LinkedHashMap<>();
                 Set<String> tNavDemand = InnerDemand.navStepDemand(t,
                         Pipelines.navSteps(t.pipeline()).keySet(), corrNav0,
-                        synthetics.allPreds(head),
-                        InnerDemand.leafChains(ops, head), predNavAliases);
+                        synthetics.allPreds(registerKey),
+                        InnerDemand.leafChains(ops, registerKey), predNavAliases);
                 Pipelines.Materialized tMat0 = tNavDemand.isEmpty()
                         ? Pipelines.materialize(
                                 t.pipeline(), tDemand0, t.classFqn())
@@ -2234,7 +2324,15 @@ public final class StoreResolver {
                                 pred, cs.mappingFqn()));
                 Pipelines.Materialized tMat = new Pipelines.Materialized(
                         tTemporal, tMat0.slotPrefixes(), tMat0.stripped());
-                boolean navToMany = !(ctx.findProperty(cs.classFqn(), SyntheticHeads.realHead(head))
+                // DOTTED (embedded) key: multiplicity comes off the NAV
+                // STEP's own stamped info — the class property lookup
+                // only speaks single-segment names
+                boolean navToMany = !registerKey.equals(head)
+                        ? !(nav.info().multiplicity()
+                                instanceof com.legend.compiler.element.type
+                                        .Multiplicity.Bounded nb
+                                && Integer.valueOf(1).equals(nb.upper()))
+                        : !(ctx.findProperty(cs.classFqn(), SyntheticHeads.realHead(head))
                         .map(pr -> pr.multiplicity())
                         .filter(mm -> mm instanceof com.legend.compiler.element.type
                                 .Multiplicity.Bounded bb
@@ -2254,9 +2352,9 @@ public final class StoreResolver {
                         navCond = cc.orientedCond();
                     }
                 }
-                NestedScope navNs = nestedScope(t, ops, head, context,
-                        chainPipe);
-                existsSubs.put(head, new Substitution.ExistsSub(navNs.pipeline(),
+                NestedScope navNs = nestedScope(t, ops, registerKey,
+                        context, chainPipe);
+                existsSubs.put(registerKey, new Substitution.ExistsSub(navNs.pipeline(),
                         navCond, t.rowVar(), t.bindings(),
                         navNs.row(),
                         t.classFqn(), Pipelines.slotAliases(t.pipeline()),
@@ -2856,7 +2954,8 @@ public final class StoreResolver {
         final ClassSource cs = Pipelines.narrowFrameSource(cs0, paths);
 
         NavPlan navPlan = registerNavigations(cs, paths,
-                InnerDemand.occurrenceSplitChains(filterPaths, projectionPaths));
+                InnerDemand.occurrenceSplitChains(filterPaths, projectionPaths),
+                filterPaths);
         Set<String> dateAliases = InnerDemand.navDateAliases(
                 chainSpecs.values(), navPlan.navHeadByAlias());
         Set<String> demanded = navPlan.demanded();
diff --git a/core/src/main/java/com/legend/resolver/Substitution.java b/core/src/main/java/com/legend/resolver/Substitution.java
index b04b9940..a9ed3fba 100644
--- a/core/src/main/java/com/legend/resolver/Substitution.java
+++ b/core/src/main/java/com/legend/resolver/Substitution.java
@@ -891,11 +891,16 @@ final class Substitution {
                     return rewriteEmbeddedExists(pl, partial);
                 }
             }
-            if (headPath != null && headPath.size() == 1
-                    && target.existsSubs().containsKey(headPath.get(0))
-                    && isEmptinessFamily(call)) {
-                return rewriteExists(call, target.existsSubs().get(headPath.get(0)),
-                        List.of());
+            // single-segment heads AND dotted embedded chains
+            // ($f.bridge.employees->exists — the embedded-union burn)
+            // dispatch identically: the sub registered under the
+            // consumer path's own spelling
+            ExistsSub dottedSub = headPath == null ? null
+                    : headPath.size() == 1
+                            ? target.existsSubs().get(headPath.get(0))
+                            : null;   // PROBE: dotted off
+            if (dottedSub != null && isEmptinessFamily(call)) {
+                return rewriteExists(call, dottedSub, List.of());
             }
             // emptiness over an INLINED derived CONCATENATION of navs
             // (Person.addresses = $this.address->concatenate(
