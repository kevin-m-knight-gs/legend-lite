# MASTER — consolidated adversarial type-system audit of legend-lite

Synthesis slug `S1-synthesis`. Inputs: **37 files** in `/home/user/audit/findings/` — 35 auditor
reports (`A01`–`A33`, `A36`, `A37`; `A34` and `A35` never landed) and **both falsifiers**
(`V1-falsifier.md`, `V2-falsifier.md`) — plus the bulk artefacts `A07-pure-registry.dump.tsv`,
`A20-fuzz-log.txt`, `A21-fallback-census.csv` and A30's three appendices, and
`/home/user/audit/CONFIRMED.md` (**V1–V39**, reproduced personally by the orchestrator).

**Status of the fleet.** **Both falsifiers landed** and between them adjudicated **202 rulings**,
each re-deriving every repro from scratch with its own fixtures.
`V1-falsifier` took `CONFIRMED.md` V1–V25 and A01–A05, A07, A08, A10, A13, A14, A17 — **91 rulings:
CONFIRMED 82 (24 duplicate-tagged) · CONFIRMED-BUT-OVERSTATED 5 · NOT-REPRODUCED 1 ·
NOT-REPRODUCED+MISATTRIBUTED 1 · FALSE-"VERIFIED-SOUND" 1 · BY-DESIGN 0.**
`V2-falsifier` took A06, A09, A12, A15, A16, A18, A20, A22–A24, A27–A30, A32 and five
`CONFIRMED.md` items, on DuckDB 1.5.0, SQLite 3.47.1 **and** H2 2.1.214 — **111 rulings:
CONFIRMED 103 · CONFIRMED-BUT-OVERSTATED 2 · DUPLICATE 6 · NOT-REPRODUCED 0 · MISATTRIBUTED 0 ·
BY-DESIGN 0.**
**Zero findings were ruled BY-DESIGN. Two were ruled NOT-REPRODUCED — and one of those two is a
`CONFIRMED.md` *correction* that retracts a claim no auditor ever filed.** §6 applies every verdict;
the seven overstatements and the false VERIFIED-SOUND entry are corrected in place in §1.
A34 and A35 remain unassigned and are declared in §5c.

**The three things that changed most since the first draft.**
1. **A30 built the independent signature oracle V8 said did not exist**, by cloning
   `finos/legend-pure` and `finos/legend-engine` and diffing all 721 catalog signatures against
   24 172 real declarations: **25.4 % of the catalog diverges from the language it claims to
   implement** (D81). Several defects the fleet found in the *compiler* are catalog defects (RC-9).
2. **A36 ran the mapping matrix exhaustively** — 874 cells (23 Pure property types × 2
   multiplicities × 19 accepted column spellings, each run 3×). Headline: **100 silently unsound ·
   12 ICE · 108 sound · 628 rejected at compile · 26 loud at runtime** — and **117 of 117 executable
   `[1]` cells deliver `null`, including the perfectly-matched diagonal** (V39).
3. **A37 ran the test suite** (which the standing brief had forbidden): **4 278 tests, 0 failures**,
   and of 19 findings put to a coverage check, **0 COVERED · 3 PINNED · 15 UNCOVERED** (D99, V36).
   The suite is green and structurally cannot see the seam this audit is about.

**How to read a row.** Every claim is either (a) a quoted `file.java:LINE` an auditor read, or
(b) a repro whose actual output the auditor pasted. Where a row says "orchestrator-verified", the
orchestrator independently reproduced it (`CONFIRMED.md` V-number). Nothing here is extrapolated
from a sample unless the row says so.

---

## 1. MASTER DEFECT TABLE (deduplicated)

**105 distinct defects** — S1 41, S2 27, S3 19, S4 12 (+1 META), S5 5.

Where several auditors found the same defect from different angles, the row is merged and the angles
are listed in *Manifestations*; a single row carries between 1 and 13 separate auditor findings
(D31 alone merges fifteen mechanisms found by nine auditors). The raw finding count across the 35
auditor reports is ~620, so the table is a ~5.9x deduplication, and `V1-falsifier` independently estimated
that its own 91 adjudicated claims cover "roughly 58 distinct defects" — the same ratio. The table
sits above the brief's 40–70 target because fourteen further auditors and both falsifiers landed
after that target was set; merging further would have meant putting distinct root sites with
distinct fixes on one line. Severity scale is defined in §2.

Legend for *Manifestations*: `A0n` = that auditor's report file; `Vn` = orchestrator repro.

### S1 — CRITICAL: silently wrong VALUES or TYPES delivered to a caller from ordinary source

| ID | Sev | One-line claim | Root site (file:line) | Manifestations (auditors / repros) | Orch-verified |
|---|---|---|---|---|---|
| D01 | S1 | The relational mapping performs **no** property-type/column-type compatibility check — there is only a coercion table, and where the coercion is a no-op the declared Pure type is a pure relabel of the driver object — *S1 because a `String[1]` property delivers `java.lang.Integer` from ordinary model text*. | `normalizer/MappingNormalizer.java:2389-2477` (`coerceColumnToDeclared`), with three disagreeing siblings `:2363` (`declaredAssertion`), `:2291` (`coerceToDeclaredNumeric`), and a 4th view path where `findPhysicalColumn` returns null (`:2400`) | A18 (36 modelled mismatches executed: **11 unsound**, 1 leaks `org.duckdb.JsonNode`, 2 silently lose data, 2 carrier drift, 6 caught); A08 (`String[1]`/`Float[1]`/`Decimal[1]` over INTEGER → `Integer(7)`); A12; A18 "same mismatch, 3 routes, 3 outcomes" | **Y — V26** |
| D02 | S1 | `cast(@T)` **converts** instead of asserting, and an intermediate widening cast makes the next narrowing cast a no-op, so the declared type and the decoded Java type diverge — *S1 because `2.7->cast(@Integer)` silently returns 3 and `1->cast(@Number)->cast(@Float)` returns `java.lang.Integer` under `Float[1]`*. | `lowering/CastPolicy.java:70-97` (converting arm), `:181-206` (`crossKindRaise`), `:216-225` (`isWidening`); `compiler/spec/CastChecker.java:23-41` (no relatedness check) | A16 (all **144** 12×12 primitive pairs classified; the IDENTITY cells are enumerated); A10; A03 (enum→String, Integer→String); A17-F8 (a relation cast rewrites the DATA: 30/4 → 8); A11-F5 | **Y — V2, V3** |
| D03 | S1 | Three Typer sites hand out `Any`, and nothing downstream ever re-checks it, so `cast(@Any)->cast(@T)` is a universal launderer — *S1 because an `Integer[1]` column is delivered holding `String(John)`*. | `compiler/spec/Typer.java:2922-2937` (late-bound `trustedColumn`), `:1113-1116` (mixed row cells → `Any`), `:2558-2565`; `compiler/element/type/Type.java:511-514` (`trustedColumn` = `Any[0..1]`) | A05-7 (sites A/B/C); A11-F5 (raw-grid: `Integer[*]` holds `BigDecimal(1.7)`, `Boolean[1]` holds `Integer(42)`, cast enforcement **0 %**); A09 | **Y — V2** |
| D04 | S1 | A generic user function's declared return type is **never** checked against its body: `unify(TypeVar, actual, new Bindings())` always succeeds — *S1 because a `String` literal is delivered in a column the compiler publishes as `Integer[1]`*. | `compiler/spec/InferenceKernel.java:540-544` (bind-anything) + `compiler/spec/Typer.java:3164-3171` (`requireConforms` with throwaway `Bindings`), driven from `SpecCompiler.java:152` | A04-1 (kernel-level `K4.java` §B confirms `unify(T,String,new Bindings())` is accepted); A16 (bodies are also unchecked at `compileModel` time). **V2-falsifier correction**: for a *monomorphic* function the declared return IS checked, at G½ (`in function 'fn::narrowRet': declares return type Integer but body returns String`); the genuinely unchecked case is the **generic** one, where no error ever fires. The two must not be conflated — the late-firing monomorphic check is the separate, lesser defect | **Y — V18** |
| D05 | S1 | Capture-unsafe β-substitution at **two independent sites**: a free variable produced by the let-inliner lands under an inner binder of the same name and is captured — *S1 because `|[10]->map({x| let y=$x; [7]->map(x|$y)->toOne();})` returns **7** where the correct answer is 10, silently*. | `compiler/spec/SourceSubst.java:102-122` (let-inlining removes the shadowed name from the env but never α-renames the inner binder); `compiler/spec/UserCallInliner.java:529-530` (`if (env.isEmpty())` skips α-renaming) + `:100-107` (`reserveFreshNames` scans only the query body) | **Repro corrected by V2-falsifier** — use A06-1's or the falsifier's own (`map(x|...)` inner binder, control binder `w` → 10). **V24's published repro and mechanism sentence are wrong and must not be shipped** — see §6a. A16 (zero-param callee + `_i0` capture → 2000 instead of 1005, independently confirmed); A06 also manufactures **false** compile errors this way | Y — V24 (defect), **repro superseded** |
| D06 | S1 | The constant folders disagree with the un-folded path: integer `plus` overflows silently, `equal`/`in`/`indexOf` compare with Java `Object.equals`, and `size()` is folded from the *static* multiplicity — *S1 because byte-identical source yields answers differing by 2^64, `1 == 1.0` folds to FALSE, and `->size()` returns 0 directly but 1 through a one-line user function*. | `compiler/spec/StaticFold.java:254-256` (unchecked `sum`), `:532-539` (`a.equals(b)`), `:303-310`, `:332-339`; `compiler/spec/NormalizeFolds.java:66-78` (static-multiplicity `size`) | A06-2/3/5. Reachable **without** any stereotype through `Typer.java:620-629` (the `.columns->map(...)` fold hook) | N |
| D07 | S1 | `collection::first(set, count)` silently drops its `count` argument; the 2-arg overload resolves and the 1-arg lowering rule fires — *S1 because `[1,2,3]->first(2)` returns one element with no error*. | `lowering/Scalars.java:1407-1411` (one rule registered for **every** key at the name `first`) | A06-7 (`take(2)` — the correct sibling — emits `array_slice(...,1,2)`); A14 (the `first`/`last` asymmetry is invisible until execution) | **Y — V25** |
| D08 | S1 | n-ary `concatenate` over a collection literal never re-checks each element against the accumulator: columns are silently transposed and a subset schema is silently widened — *S1 because a column declared `Integer[1]` delivers `String("z")`*. | `compiler/spec/ConcatenateChecker.java:31-46` + `compiler/spec/InferenceKernel.java:1363-1372` (`unionRows` is order-insensitive) | A13 (2-arg form correctly rejects the same pair; adding one list element turns the error into a wrong answer, or into `Binder Error: Set operations…`) | N |
| D09 | S1 | `match` takes the wrong arm: dispatch ignores generic type ARGUMENTS and relation column lists, and `MatchFold` re-implements the exact first-statically-conforming rule `TypedMatchRuntime` exists to prevent — *S1 because `list([1,2])->match([List<String>…, List<Integer>…])` returns the String branch and `'x'->cast(@Any)->match([String…, Any…])` returns the `Any` arm*. | `compiler/spec/InferenceKernel.java:1094-1122` (`paramTypeScore` compares raw FQN only); `lowering/MatchFold.java:28-37` reached from `lowering/Lowerer.java:3122-3124` | A03 (19 antisymmetry violations of this class over 42 constructible types); A16; A22 | N |
| D10 | S1 | A property mapping's table qualifier is ignored — any unresolvable table silently reads the MAIN table — *S1 because `[db] U.A` returns `T.A`'s value with no diagnostic*. | `normalizer/RelOpTranslator.java:103-116` (`if (base == null) base = tableScope.get(defaultTable);`) | A18 (three cases run: bogus table, real unjoined table, bogus database — all read `T.A`). The sibling `ColumnRef` arm at `:205-218` is loud | N |
| D11 | S1 | `RawSqlBoundary.h2ToDuckDb` regex-rewrites **inside string literals**, silently changing user data on every DuckDB `executeInDb` — *S1 because a user's `'CURRENT_TIMESTAMP()'` literal comes back as `'CURRENT_TIMESTAMP'`*. | `sql/dialect/RawSqlBoundary.java:151-185` (five unanchored regexes, no lexer), wired unconditionally by `RawSqlAdapt.java:22-31` + `DuckDb.java:85-88` | A11-F6 (5 rewrites in isolation + 2 end-to-end); also mutates `WHERE` predicates and `DEFAULT` clauses | N |
| D12 | S1 | `Checkers.prefixedUnion` uses exact-name uniqueness while the type system's column identity is quote-insensitive, so a prefixed join mints two "same" columns and a by-name read silently picks the wrong one — *S1 because `select(~[r_ID])` returns 777 (the left side) instead of 1*. | `compiler/spec/Checkers.java:56-67` vs `compiler/spec/InferenceKernel.java:442-447` (`sameColumn`) | A15 (the non-prefix path catches the identical pair loudly); related to D64 | N |
| D13 | S1 | Pivot: `RelationType.pivotColumnType` matches the aggregate template with `lastIndexOf`, so a template whose name is a suffix of another steals it and the column gets the wrong Pure type; NULL-key rows are silently dropped; the static-pivot emulation names columns with a Java record `toString()` — *S1 because a column declared `Integer` holds a `Double`, and measures for NULL keys vanish from the result entirely*. | `compiler/element/type/Type.java:491-503`; `exec/DynamicPivot.java:27-28, 77`; `sql/dialect/CarrierStrategies.java:301-307` used at `:157` | A01 (`2011__|__a__|__b` declared INTEGER, cell is a Double); A14 (150 trees in, 100 reported); A11-F15; A13 | N |
| D14 | S1 | The GRAPH JSON envelope destroys every Decimal (`to_json(list(json_object(...)))` re-encodes through DOUBLE) and `Compiler.execute` and `Compiler.executeStreaming` therefore return **different JSON documents** for the same query — *S1 because `12345678901234567890.1234567891` arrives as `12345678901234567000.0`*. | `sql/dialect/AnsiSqlRenderer.java:485-496` (`jsonArrayAgg`); streaming path `lowering/StreamingGraphRoot.java:27-41` is the correct one | A19 (isolated DuckDB proof of all four idioms + product path + `EQUAL=false` between the two execute surfaces); A09-F13 | N |
| D15 | S1 | There is **no SQLite dialect object** — SQLite inherits `AnsiSqlRenderer` (identity `normalize`, ANSI type names, **DuckDB function spellings**), so one static type carries corrupted values on that backend — *S1 because `Boolean[1]` carries `Integer`, `Decimal[1]` carries `Double`, temporal casts carry `Integer(0)`, and `toString()` on a temporal returns `null` on every row*. | `sql/dialect/Lexicon.java:32-41` ("its class is gone"); `Compiler.java:561-564` hands SQLite `Spellings.DUCKDB`; only `sql/dialect/H2.java:475` overrides `normalize` | A11-F1/F2/F3/F4/F7/F8/F9/F17; A09-F2 (`PureAsserts.equalScalar` returns **false** between the DuckDB and SQLite decode of one query); A14 (`plus()` over a DATE returns `Double(2020.0)`) | **Y — V21** |
| D16 | S1 | H2 decode defects: `java.sql.Date.toLocalDate()` shifts every date before 1582-10-15 (Julian drift, up to 10 days), `H2.normalize` narrows a full-precision `BigDecimal` to `double`, and `booleanShaped`'s `default -> false` makes `CAST(bool AS VARCHAR)` render `TRUE` instead of `true` — *S1 because the value is silently wrong under an unchanged declared type*. | `exec/Executor.java:667-668`; `sql/dialect/H2.java:485-488`; `sql/dialect/H2.java:454-469` | A09-F1 (33 dates swept, **16 BAD**; the TIMESTAMP lane of the same engine is correct, which is the proof it is our decode) and F14 (21 significant digits discarded by our code, not the DB); A11-F15 | N |
| D17 | S1 | A subclass may override an inherited property with a **disjoint** type or a **narrowed** multiplicity; nothing compares a declaration against the one it shadows — *S1 because the same DB row returns `String("007")` through the supertype and `Long(7)` through the subtype*. | `compiler/element/ClassCompiler.java:43-52`; `compiler/element/ModelIntegrity.java:101-138` (per-class `HashSet`, never consults supers); `compiler/element/PureModelContext.java:186-208` | A07 (all five edit directions accepted, none diagnosed); A03 (`readAsString(^D(p=1))` typed `String[1]`, returns `Integer(1)`) | N |
| D18 | S1 | Any user model can silently **redefine a stdlib native** at 413 of the 431 native FQNs and the user's overload wins end-to-end, taking the TYPE with it; enums silently win, classes silently lose — *S1 because `[1,2,3]->first()` returns `String(HIJACKED)` typed `String[1]` instead of `Integer[0..1]`*. | `compiler/element/FunctionCompiler.java:34-79` (suppresses only the 18 `PlatformTypes.isPlatformOwnedFunction` FQNs); `compiler/element/TypeClassifier.java:69-78` (class: native first; enum: **user** first) | A07 (18/431 owned; the `System.err` note is not a diagnostic channel); A03 (a user enum replaces the platform's 12 `Month`s for the whole compile) | **Y — V23** |
| D19 | S1 | The `$` sigil **is** user-writable, so a function named `<Class>$prop$<name>` silently hijacks a derived property and `<Class>$constraint$<name>` silently replaces a constraint predicate — *S1 because `'DERIVED'` becomes `'USER-WINS'` with no diagnostic anywhere*. | `lexer/Lexer.java:192-194` (`isIdentPart` includes `'$'`) contradicting `compiler/SynthFqn.java:17-19` and `compiler/ModelBuilder.java:299-301` | A17-F4 (both the derived-property and the constraint form run); escapes `checkDuplicateSignatures` for the reason in D66 | N |
| D20 | S1 | Cross-kind `==` type-checks and returns TRUE — *S1 because `1 == '1'` and `Month.January == 'January'` are `true`, and `'a' == 1` escapes as a raw JDBC exception*. | `equal` declared `(Any[*],Any[*]):Boolean[1]` in `builtin/Pure.java`; `EnumType` has no supertype so no lattice check can reject the pair; lowering hands both sides to the DB | A03 (enum-vs-enum **is** folded to FALSE, so only enum-vs-String and Integer-vs-String are broken); A10 | **Y — V11** |
| D21 | S1 | Decimal literal typing is **precision-blind** (`decimalType` validates only the scale) and every over-scale decimal is silently HALF_EVEN-rounded because the "explicit D-suffix keeps the loud reject" guard is dead — the parser strips the suffix before storing `written` — *S1 because `Decimal(38,s)` is stamped on a 45-digit value that then decodes as `java.lang.Double`, worst case `Infinity`*. | `compiler/spec/Typer.java:3178-3185` (`decimalType`) and `:156-174` (the dead guard); `parser/SpecParser.java:845-851` (suffix stripped); compounded by `sql/SqlTyping.java:1195-1204` re-inferring `precision>38 → DOUBLE` | V4; A01; A05-1/3; A06-13 (a **scale-1** case, so no rounding path is involved — the defect is in the *precision* computation); A17-F1/F2; A22 (node-level: `precision=45` under a declared `precision=38`); A10 | **Y — V4** |
| D22 | S1 | Declared and emitted decimal scale diverge: `refineDecimalCarrier` stamps `Decimal(38,18)` while the lowering casts to a different scale, and the normalizer's numeric-over-string arm hard-codes `DECIMAL(5,2)` — *S1 because `'1.23456789012345678901'` under a `Decimal[1]` property silently becomes `1.23`, and `>= 1000` is a hard runtime failure*. | `compiler/spec/Typer.java:1814-1826` + `lowering/Scalars.java:2320-2327` (`new SqlType.Decimal(5, 2)`) | A06-14; A18 (`SELECT CAST(rtrim(V,'dD') AS DECIMAL(5,2))`, both the truncation and the `Conversion Error`) | N |
| D23 | S1 | The per-hop `(INNER)`/`(LEFT)`/`(OUTER)` join-type annotation on a mapping property-join chain is parsed, stored and **completely ignored** — every hop is LEFT-joined — *S1 because rows an INNER hop would drop are returned with NULLs instead*. | `model/JoinChainElement.java:25` populated at `model/MappingFromProtocol.java:530`; exhaustive grep: the only behavioural reader is `FilterMapping.JoinMediated.joinType` (`normalizer/MappingNormalizer.java:2059, 2846`) | A15 (the `(INNER)` and plain chains render to the *identical* alias `t2.CNAME`, i.e. deduplicated as if the annotation did not exist); `JoinType.OUTER` has no `JoinKind` counterpart at all | N |
| D24 | S1 | `includeType` emits the **static** class, not the runtime class, and `includeEnumType` fabricates the string `"ks::Colour."` for a NULL enum — *S1 because the discriminator whose only purpose is class recovery says `poly::Animal` for a `Dog` and a `Cat`, and a non-null value appears where the value is empty*. | `lowering/Lowerer.java:848-851` (`g.classFqn()` is the fetched class); `resolver/GraphEmission.java:3351-3374` (concat's null-skip, admitted at `:3348-3350`) | A19 (the plan literally projects `TRUE AS "stc_poly__Dog___$member"` and ignores it) | N |
| D25 | S1 | `tdsRowCellIndexRead` truncates the cell index to `int`, so a 2^32-scale index silently reads a **different column**, and a non-literal index degrades to the identity read (the whole ROW typed as one cell) — *S1 because `->at(4294967297)` runs successfully and returns column `b`*. | `compiler/spec/Typer.java:1080-1092` (`ki.value().intValue()`) and `:1051-1069` (row-major arm, `[1]`-stamped with no guard) | A05-5/6/9; A06-15; the out-of-range case additionally raises a raw `IllegalStateException` (see D50) | N |
| D26 | S1 | Overload dispatch by `typeName().endsWith(...)`: the signature-mangle tail is not injective, so an explicitly-named overload resolves to a **different** one and the call is typed accordingly. | `compiler/spec/Typer.java:2304-2308` (`f.returnType().typeName().endsWith(String.valueOf(ret))`); `SignatureMangle` has no `mangle()`, only a demangler | A04 (3 repros: `my::h_Number_1__String_1_(1)` dispatches to the `Integer→MyString` overload; `StrictDate`.endsWith(`Date`)); root cause is `typeName()` non-injectivity (D67) | N |
| D27 | S1 | Pure's empty-collection identities are implemented in the SCALAR lane and **not** in the AGGREGATE lane — the same Pure function gives two answers — *S1 because `[]->and()` is `true` in one lane and `NULL` in the other under a declared `Boolean[1]`*. | `lowering/Scalars.java:186-219` (COALESCE identity) vs `lowering/Aggregates.java` → `Lowerer.aggExpr` (bare `BOOL_AND`/`STRING_AGG`/`SUM`) | A14 (one DuckDB session, both lanes side by side, for `and`/`or`/`joinStrings`); A07 (`sum([])` is 0 in Pure); `lowering/Stamps.java:26-36` claims "THE FORK IS CLOSED" | N |
| D28 | S1 | `==`/`eq` deliberately omits the null guards that every ordering operator applies, so a nullable operand leaks SQL three-valued NULL into a column typed `Boolean[1]` — *S1 because the codebase's own convention for an empty operand is `false` (what `>` does in the same projection)*. | `lowering/Scalars.java:84-156` (comment at `:79` states the omission is deliberate) vs `NullSemantics.optionalOperandGuards` used at `Scalars.java:172-182, 598-604, 849-866, 2120-2130` | A10 (three operators, three null disciplines, one declared type); V1 | **Y — V1** |
| D29 | S1 | `compatibleRebind`'s "Any escape hatch" lets an `Any`-typed argument enter a type variable already bound to a narrower type; the narrow binding survives — *S1 because `['a','b']->concatenate([1,'x'])` is typed `String[*]` and returns `1` coerced to `"1"` and `'x'` **with its quotes**, and `[1,2]->concatenate([1,'a'])` is typed `Integer[*]` with a String provably in the list*. | `compiler/spec/InferenceKernel.java:1406-1409` (`if (isAny(existing) \|\| isAny(actual)) return true;`) falling out of `:505` without rebinding | A04-2 (kernel-level: binding a relation row then `Any` silently erases the schema) | N |
| D30 | S1 | `Any`-collection and heterogeneous-enum carriers collapse enum identity to bare strings, and enums decode to `java.lang.String` on every path — *S1 in the enum-list case because which enumeration a value belonged to is unrecoverable after encode*. | `lowering/Lowerer.java:2443-2453` (`to_json(name)` carrier); `lowering/PureSql.java:129` (`EnumType -> VARCHAR`); `exec/Executor.java:662-678` has no enum arm | A10; A09-F19; A19 ("Enums are lexically indistinguishable from Strings on every path") | N |

| D76 | S1 | The declared precision and scale of a property are **destroyed** between model and compiler: `NameResolver` rebuilds a `Generic` through the 2-arg constructor and `Type.GenericType` has no slot for type-variable values, so `amount: Decimal(18,4)[1]` becomes the degenerate type `Decimal<>` that no decimal-handling site recognises — *S1 because the most natural money declaration makes a legal model uncompilable, and `Decimal(10,2).equals(Decimal(38,18))` is `true`*. | `compiler/NameResolver.java:452-457` (2-arg `Generic` ctor drops `typeVariableValues`/`multiplicityArguments`/`pos`); `protocol/TypeExpression.java:123-135` (`Generic.equals`/`hashCode` exclude `typeVariableValues`); `compiler/element/TypeClassifier.java:101-107` (`Type.GenericType` has no tvv slot) | V33; A26 (wire vs model matrix — **the WIRE keeps what the model loses**; `[PLAN-ERROR] property 'amount': expected Decimal<>, got Decimal(18,4)`; removing `(18,4)` makes the same model compile); A17-F14 | **Y — V33** |
| D77 | S1 | Every numeric field at the protocol ingress is narrowed from the wire's `long` to `int` with no range check, fabricating static Pure types, constructing unrepresentable multiplicities, and truncating an emitted literal's sign and magnitude. | `model/FromProtocol.java:310, 343-344, 350-351` (size/precision/scale); `model/MappingFromProtocol.java:605-612` (multiplicity bounds); `protocol/ProtocolEmitter.java:3172-3174` (`foldNegation` via `longValue()`); `protocol/Multiplicity.java:66-74` never validates `upperBound` | A26 (17 narrowing sites swept, 3 defects + 12 sound + 2 noted): wire `DECIMAL(4294967298,4294967297)` → static `PrecisionDecimal(2,1)`; `VARCHAR(2147483648)` → `Varchar[size=-2147483648]`; `+localProp: String[4294967296]` → `[0]` silently, `[0..2147483648]` → **`[0..-2147483648]`**, `[3000000000]` → raw `IllegalArgumentException`; A02 (cited the multiplicity site from source — now confirmed with three failure modes) | N |
| D78 | S1 | The DDL/seed lane is a sixth, disagreeing Pure→SQL type mapping that silently changes the data it writes: `CsvSeed` pins **every** decimal column to `DECIMAL(38,9)`, execution DDL drops `NOT NULL` and `PRIMARY KEY`, Pure `Integer` (64-bit) is emitted as 32-/16-/8-bit, Pure `Float` (a double) as 4-byte `REAL`, Pure `String` (unbounded) as `VARCHAR(n)`, and a numeric CSV cell is spliced into the INSERT raw and unquoted. | `exec/CsvSeed.java:144-147`; `exec/Ddl.java:62-98` (no constraints), `:425-440` (widths), `:330-333` (raw splice); `compiler/element/StoreCompiler.java:170-171` derives the `[1]` the DDL then does not enforce | A27 (six independent mappings censused; `Ddl.spell` vs `duckType` vs `dataTypeToSqlText` vs `PlanText.spell` vs `CsvSeed.ddlType` vs `PureSql.type`+`TypeNames` all disagree; `setUpDataSQLs` has **two** mappings so the text a golden asserts is not the DDL that executes) | N |
| D79 | S1 | `generateTestData`'s extract→CSV→re-ingest loop is not idempotent and silently loses rows and characters: the CSV scrub rewrites `,`→`;`, `'`→space and newline→space with no marker, every fetch ends in an unconditional `limit 20`, and three different CSV parsers disagree on the same cell. | `testdatagen/TestDataGenerator.java:1088-1093` (scrub), `:276, :344, :370` (`limit 20`), `:1264-1270` (parser 3); `exec/Ddl.java:287-311` (parser 1, quote-aware); `exec/CsvSeed.java:93, :104` (parser 2, `split(",",-1)`+`strip()`) | A27 (25 requested row identifiers → 20 returned; `assertTestData` then fails on the round trip; empty string, whitespace-only and `---null---` all become SQL NULL; an unknown table or column silently produces a plausible-looking seed) | N |
| D80 | S1 | `executeWire` and `executeStreaming` silently **drop** dynamic pivot columns that `execute` returns, and `executeWire`'s declared column names disagree with its own wire body's keys. | `exec/DynamicPivot.staticize` is applied only at `StatementExecutor.java:2329-2331`; `Compiler.lowerQuery:345-368` (the shared front for `plan`/`planStreaming`/`executeStreaming`/`executeWire`) never calls it; `Compiler.java:432-439` vs `WireRender` | A25 (same pivot query: `execute` returns 4 columns incl. `'NYC__|__total'`, `executeStreaming` returns 2; CSV fails where JSON succeeds on the same query) | N |

| D91 | S1 | Structural equality **erases class identity**: two instances of unrelated classes with the same property shape compare `true`, a subclass equals its superclass, and `<<equality.Key>>` is ignored entirely on the ordinary execute path — *S1 because `^model::A(x=1,y='p') == ^model::B(x=1,y='p')` returns `true` and a keyed class compares its non-key properties*. | The execute path lowers instance equality to a bare struct-to-struct comparison (`SELECT {'x':1,'y':'p'} = {'x':1,'y':'p'}`); neither class FQN appears in the SQL. `lowering/InstanceEquality.java` — the key-respecting comparator — is armed only on the assert-verdict lane | V38; A28 (four spellings: `==`, `eq(...)`, `->contains(...)`, sub-vs-super — all `true`; `^K(k=1,n='a') == ^K(k=1,n='b')` with only `k` keyed → `false`); **V2-falsifier: CONFIRMED** (own fixtures) | **Y — V38** |
| D92 | S1 | **No scalar function null-checks its input**: 135 declared-`[1]` results return NULL, `hasDay`/`hasHour`/… on a *column* emit an INTEGER under `Boolean[1]`, `times([Integer])` returns a `Double` under `Integer[1]`, `plus([true,false])` returns a `BigInteger` under `Boolean[1]`, `lite::hash(String[1]):String[1]` returns a `Long`, `date::quarter():Quarter[1]` returns a number rather than the enum, and `asin`/`acos` fabricate `NaN` from NULL. | The scalar rule table (`lowering/Scalars.java`) emits the bare call for every one of these; `Executor.unwrap` has no arm asserting the decoded class against the declared type | A24 (full scalar matrix, declared vs actual); **V2-falsifier: CONFIRMED** on the sampled set — `$p.d->hasDay()` → `SELECT 1 AS x` → `java.lang.Integer(1)` under `Boolean[1]` while the literal path gives `SELECT TRUE` → `Boolean(true)`; `\|times([2,3,4])` → `Double(24.0)` under `Integer[1]`; `\|lite::hash('abc')` → `Long(...)` under `String[1]` | N |
| D93 | S1 | Temporal (milestoning) rows are silently mis-selected: a temporal class over a table with **no** milestoning block is silently unfiltered, open-ended (`thru IS NULL`) version rows are invisible at **every** date including `%latest`, and the snapshot-column type defaults to "is a date" when the column is absent from the row. | `resolver/TemporalFrame` / `resolver/Substitution` — the frame is skipped rather than walled when the block is missing | A32 (findings 11, 12, 13). Silently returns a **different row set**, which is the worst shape a temporal defect can take | N |

| D100 | S1 | `cache/HandleStore`'s key does not determine its value: two unrelated models that share one connection definition get **each other's tables**, and the compiler's static type is violated by the rows returned. | `cache/HandleStore` (keyed on connection text, not on the model/store identity); the store never evicts | A31-F1 (the public API surface). This is the only defect in the audit where one *caller's* data reaches another caller | N |
| D101 | S1 | Relation `sort` claims Pure's "null is largest" ordering and emits **no** ASC null clause, relying on the backend default — and **two of the three backends invert it**; the window `ORDER BY` in the *same statement* does emit `NULLS LAST`, so one rendered query can order two ways. | `lowering/Fold.java:377-385` (`sortNulls` returns `null` for ascending) vs `lowering/Lowerer.java:2177-2178` (window emits `NULLS_LAST` explicitly) | A13 (DuckDB nulls last, SQLite nulls first, in one statement); **V1-falsifier CORRECTED A13's H2 row — H2 also orders nulls FIRST**, so the documented contract holds only on DuckDB, by luck of that backend's default. The finding is *understated* in A13, not overstated | N |
| D102 | S1 | `graphFetchChecked` reports **zero defects** for an object that violates every constraint, because a SQL NULL is not `false`; and `Base.all()` under an `inheritance` Operation silently ignores the declared member sets and drops the base class's own extent. | `resolver/GraphEmission` (the checked-envelope defect predicate); the inheritance-Operation route in `normalizer/UnionSynthesis` | A33 (both run end to end). Two independent silent-wrong-answer routes on the graph lane | N |

### S2 — HIGH: a declared type/multiplicity the runtime violates, or an unchecked boundary that licenses it

| ID | Sev | One-line claim | Root site (file:line) | Manifestations (auditors / repros) | Orch-verified |
|---|---|---|---|---|---|
| D31 | S2 | **A declared multiplicity is never enforced at any egress** — neither the lower bound (NULL under `[1]`) nor the upper bound (`[2]` returning 3 values) — and the type system's own correct answer is repeatedly overwritten on the way. This is the single largest defect family in the audit. | Egress: `exec/Executor.java:301-308` (SCALAR checks *zero rows* only), `:370-391` (COLLECTION checks lower only), `:394-400` (GRAPH checks nothing), TABULAR has no check at all. Twelve manufacturing sites listed below. | **12 distinct mechanisms**, all executed: (a) synthesized `trustOne` over a physically-nullable column — `MappingNormalizer.java:3394-3396` + `lowering/Scalars.java:485-490` (identity, no guard) — A02, A12-H3, A18, A08; (b) LEFT/RIGHT/FULL/ASOF join copies the right side's multiplicities verbatim — `InferenceKernel.java:823`, `Checkers.java:68`, `resolver/Pipelines.java:425` & `:560`, `StoreResolver.java:2036` — A15, A12-H2, A20 (8 independent fuzz shrinks); (c) association navigation over a dangling or nullable FK, same sites — A15 (4 repros incl. milestoned + graph), A02, A12; (d) 21 registry signatures / 18 executed aggregates declare `[1]` and return SQL NULL over an empty or all-NULL group — `builtin/Pure.java:2187-2189, 1180, 1894, 1919-1921, 2237-2244, 2175-2178, 1987, 1178, 1899, 2001, 2208, 1893` — A07, A14, A20 (**960 of 1010** oracle violations), V19; (e) user `->toOne()` over a `[0..1]` falls to the default identity arm — `lowering/Scalars.java:434-481` — A02, A20; (f) object-space `->toOne()` is **deleted** from the chain — `resolver/StoreResolver.java:2587-2590` — V20, A12-H1, A19 (graph: 0 rows → `[]`, 2 rows → 2 objects); (g) `match` on `[0..1]` against a `[1]` branch — `MatchChecker.java:288-296` — A16; (h) `cast(@Relation<(c:T[1])>)` ignores multiplicity in its identity test — `Lowerer.java:3238-3243` — A16; (i) a derived/qualified property on a `[0..1]` receiver takes the property's declared `[1]` — `Typer.java:2789-2799`, `:537-546` — A05-4; (j) unmapped/case-mismatched/NULL enum source → NULL under a `[1]` enum property — `MappingNormalizer.java:3028` — A18, A19, A08; (k) a union branch that does not map a property contributes `trustOne(cast([],@T))` — `UnionSynthesis.java:1176-1194` — A18; (l) `^Class()` never checks that required `[1]` properties were supplied — `NewChecker.java:67-166` — V27, A16. Upper bound: A02 (`[2]` → 3 values, `size()` = 3), A19 (graph `[1]` → 0 or 2 objects) | **Y — V19, V20, V27; V1 (part)** |
| D32 | S2 | `Multiplicity.product` overflows raw `int`: an ordinary two-hop navigation is stamped `[0]`/`[0..1]` (a fabricated cardinality) or throws a raw `IllegalArgumentException`. | `compiler/element/type/Multiplicity.java:118-119` (two unguarded `int` multiplications), reached from `Typer.java:2899 → :2946` | A02 (4 end-to-end repros + a direct algebra probe; `[0..65536]²` = `[0]`, `[0..2147483647]²` = `[0..1]`, `[50000]²` throws). The parser accepts bounds up to `Integer.MAX_VALUE` (`TokenStreamCursor.java:318-326`), so all are reachable from model text | **Y — V5** |
| D33 | S2 | `ProjectChecker.clampTdsCells` re-stamps every many-valued column `[0..1]` — including `[2]` and `[1..*]` — while the lowering does **not** honour the clamp; `ExtendChecker` does not clamp at all, so the two constructs give different multiplicities for an identical colspec body. | `compiler/spec/ProjectChecker.java:76-95` vs `Typer.java:2218` (extend keeps the lambda multiplicity) | V17; A02; A06-8; A13 (both die with `IllegalStateException: a many-valued cell reached a scalar TDS slot`) | **Y — V17** |
| D34 | S2 | `project`/`extend` accept a ROW-typed or relation-typed column; the delivered column violates **both** its declared name and its declared type, or the pipeline ICEs three phases later. | No scalar-cell check in `ProjectChecker`/`ExtendChecker`; `Type.RelationType` accepts a nested row as a column type (`Type.java:520-544`); `lowering/PureSql.java:156` throws for a relation column | V16 (`b : (a:String[1])[1]` declared, `b_a : String[1]` delivered); A13 (propagates through `select`/`distinct`/`sort`/`concatenate`; `rename` over it ICEs; a duplicate `b_a` ICEs); A01 | **Y — V16** |
| D35 | S2 | `if` over relation branches fabricates a `RelationType` that **neither** branch satisfies (the join is the column **union**, i.e. a lower bound for width subtyping) and phantom columns are selectable. | `compiler/spec/IfChecker.java:68-70` → `InferenceKernel.commonSupertype:1254-1256` → `unionRows:1363-1374` | A16 (`compileQuery` publishes a column `b:String[1]` the executed result does not have; `->select(~[b])` then raises a raw `IllegalStateException`) | N |
| D36 | S2 | The inference kernel **cannot reject a bad instantiation**: type variables are unbounded (the model has no way to express a bound), conflicting bindings are silently widened to `Number`/`Any`, there is no occurs check, `resolve` is one-level, and nothing freshens a signature's variables. | `compiler/spec/InferenceKernel.java:1070` (`TypeVar -> 0`), `:502` & `:522-525` & `:1302` & `:1305` (widening), `:540-541` (no occurs check), `:737-739` & `:783-794` (one-level resolve, unbound vars ride out), `Bindings.java:53-54` (keyed by raw name); `Type.TypeVar` is `record TypeVar(String name)` | A04-3/4/5 (kernel probes `K1.java`, `K4.java` pasted); A20 (`-aString` claims `String[1]`; `+x` is a silent identity fallback on any type; **169 of 431** BAD_SQL fuzz hits are this family); A04-6 (a polymorphic body compiled once with its declared variable types escapes to lowering as `IllegalStateException: unresolved type variable T`) | N |
| D37 | S2 | `Class X extends meta::pure::metamodel::type::Nil` makes X a subtype of **everything**, including primitives and FQNs that do not exist, because the bottom-type arm fires inside the recursion. | `compiler/element/ModelContext.java:233-258` (`if (childFqn.equals(PlatformTypes.NIL)) return true;` inside the recursive walk); `Nil` is an ordinary nameable entry at `builtin/Pure.java:178` | V10; A03 (`isSubtype(Evil, no::such::Type) = true`; `takesPerson(^Evil(...))` accepted and typed `String[1]`). Defeats every conformance gate: overload scoring, unification, `subType`, mapping class binding | **Y — V10** |
| D38 | S2 | Reference safety never classifies the `extends` list or a generic head, so an unknown supertype, a primitive/enum supertype, a phantom generic head and a conflicting diamond are all accepted silently. | `compiler/element/ClassCompiler.java:38-41` (`headFqn` returns the raw string); `compiler/element/ModelIntegrity.java:101-138` never touches `cd.superClasses()`, `:265-273` skips unresolvable heads; `compiler/element/TypeClassifier.java:101-107` (Generic arm, no `findType`) | A03 + A07 + A17-F5/F6: typo'd super → `isSubtype(D,Bass)=true` for a nonexistent type and `isSubtype(A,Any)=false`; `extends Integer` → `isSubtype(A,Number)=true` while `paramTypeScore` says no; `Zork<Integer>` reaches the Phase-G result type; `List<String,Integer>` silently stops being a list; diamond conflicts resolve by `extends`-list ORDER (`PureModelContext.java:193-197`); no user class and no enum is `<: Any` (13/35, and 20 native classes) | N |
| D39 | S2 | An **unbound multiplicity variable** `[m]` on a class property is accepted, survives to the exec boundary on one lane and ICEs on the others; `unifyMult` skips all checking when the actual is a `Var`. | `parser/TokenStreamCursor.java:1176-1179` (any identifier is a `Multiplicity.Parameter`, no scope check); `compiler/spec/InferenceKernel.java:700-719` | A02, A17-F3 (`[EXEC-COL] v : String mult=[m]`). A class cannot even *declare* a multiplicity parameter (`Class t::A<T\|m>` is a parse error), so every `[m]` is necessarily free. Contradicts `Multiplicity.java:29-32` ("post-G layers must never see a `Var`") | N |
| D40 | S2 | A lambda **result** multiplicity is checked on the upper bound only, and a `Nil` body skips the check entirely, so a partial `Boolean[0..1]` predicate (or `[]`) is accepted where a total `Boolean[1]` is declared and SQL 3VL silently resolves the gap. | `compiler/spec/InferenceKernel.java:255-275` (`unifyMultResult`, "LOWER bound stays LENIENT"); `Typer.java:2144-2151` (Nil skip) | A13 (`filter` drops the null-city row); A15 (`{l,r\|[]}` → `ON NULL`, a join that matches nothing) | N |
| D41 | S2 | Pure `Integer` has **no width**: literals beyond 64 bits type `Integer[1]`, `sum()` widens to HUGEINT, and one declared `Integer` reaches the caller as `Byte\|Short\|Integer\|Long\|BigInteger\|BigDecimal\|Double` depending on the column, the backend and the construct. | `compiler/spec/Typer.java:152` (no range check); `sql/SqlTyping.java:853-866` (SUM → HUGEINT, never propagated back to the Pure type); `exec/Executor.java:655-661` (the narrowing gate is `bitLength() < 63`, off by one bit) | V7, V19; A05-2 (`Long`/`BigInteger`/`Double(1.0E44)` under one type); A06-9 (three carriers in one query family — note V2-falsifier's nit: `max()` declares `Integer[0..1]`, not `[1]`, so that one query is honest); A07; A08 (`BigInteger(18446744073709551614)` under `Integer[1]`, census reports 0 divergences); A09-F3 (the off-by-one gate flips exactly at 2^62); A13; A14 (`((Number)v).longValue()` reads **-2**); A18 (four carriers from four column widths) | **Y — V7, V19** |
| D42 | S2 | `commonSupertype` of two `PrecisionDecimal`s returns whichever operand came **second** — a narrowing, not a join, and not commutative — so the declared scale is violated by the value and the query's type flips with source order. | `compiler/spec/InferenceKernel.java:1331-1341` (`nominalFqn` collapses both to bare `Decimal`) + `:1294-1298` (`if (ctx.isSubtype(fa,fb)) return b;`) | V12; A01 (unit matrix + end-to-end: `z` declared `Decimal(10,2)` holds 14 integer digits); A03 (6 commutativity violations over 42 types, all `PrecisionDecimal`); A20 | **Y — V12** |
| D43 | S2 | Decimal arithmetic **erases** precision and scale: the entire `Type.PrecisionDecimal` algebra has zero production callers, so `Decimal(10,2)+Decimal(10,2)` is typed bare `Decimal`, aggregates keep the *input* precision, and every backend computes a different result type. | `compiler/element/type/Type.java:200-242` (dead) + `compiler/spec/InferenceKernel.java:1395-1405` (`primitiveFqn` collapses `PrecisionDecimal → Decimal` before every overload check) | V13; A01 (`sum(Decimal(10,2))` declared `Decimal(10,2)`, returns 11 digits); A03; A11-F10 (the claimed (p,s) vs DuckDB/SQLite/H2 measured: DuckDB **overflows** on a multiplication the compiler's own rule says fits); A14; A20 | **Y — V13** |
| D44 | S2 | `#TDS` column annotations lie: `:Decimal` is always `Decimal(38,0)` — scale 0 — while cells carry fractions, and `:Number` is silently narrowed to `Float`. | `compiler/spec/TdsChecker.java:169` and `:224` (Decimal), `:168` (`case "Float","Number" -> FLOAT`) | A01; A14 (every aggregate over such a column inherits the lie; integral `Number` data round-trips as `1.0`) | N |
| D45 | S2 | Post-H root types lie: `->serialize(tree)` reports a **class** for a JSON string value, object-space scalar `->map` reports a `Relation<…>` for a column of Strings, a navigated graph root reports the wrong class, and `ResultShape.of` mis-shapes a table as a scalar on a `u_map__`-prefixed **user-writable** column name. | `resolver/StoreResolver.java:2545-2551` & `:2920-2921`; `resolver/ScalarValueReads.java:63-70` + `exec/Executor.java:392`; `exec/ResultShape.java:41-54` (prefix test against `SqlSelect.SYNTH_MAP_COL`) | A12-H5/H6; A19 (`returnType()` = `ks::Kitchen` while the payload is `ks::Inner`); A09-F4 (identical on all three backends; with >1 row it becomes an ICE) | N |
| D46 | S2 | `graphFetch`/`serialize` never validate the tree's ROOT CLASS — a class that does not exist in the model compiles and runs — and the `graphFetch` tree is ignored entirely, so `serialize` can emit properties that were never fetched. | `compiler/spec/GraphFetchChecker.java:97` (validates against the SOURCE class; `GraphFetchLiteral.className()` is never resolved) | A19 (three repros incl. `#{ks::Nonexistent{name}}#`) | N |
| D47 | S2 | Dialect selection is guessed and the two public surfaces disagree: `Compiler.dialectOf` silently returns the **DuckDB** renderer for a null or unknown runtime, `TypedFrom` invents `type = "H2"` even when a type IS spelled, and `Compiler.plan()` renders DuckDB SQL for a declared H2 runtime while `Compiler.execute()` renders H2 SQL. | `Compiler.java:510-518` (two `return new DuckDb();` fallbacks) and `:545-547` (`case H2 -> distinct.add("DuckDB")`); `TypedFrom.java:82-84` and `:131-134` | V28; A11-F11/F12 (the plan's `starts_with`/`date_part` do not exist on H2 2.1.214); A15; A22. The public 3-arg `Compiler.execute(model, query, connection)` (`Compiler.java:577`) always passes `runtimeFqn = null` | **Y — V28** |
| D48 | S2 | Type annotations the user writes are parsed, stored and then **never read**: a colspec type annotation `~[a:Integer[1]]` is discarded (and a malformed one is silently reinterpreted as an expression), and a lambda parameter's declared type/multiplicity is discarded unless the signature slot is `Any`. | `compiler/spec/Typer.java:3118-3126` & `:3129-3162` (`unknownColumn(name)` substituted); `parser/SpecParser.java:2321-2323` (`catch (ParseException) { return null; }`); `Typer.java:2055-2086` (annotation wins only for an `Any` slot) | A17-F16/F17; A02 (6 annotations, all ignored); A06-6 (`filter({p:String[1]\| $p.age > 30})` on a `Person` relation is accepted); A16 (`map` ignores it, `eval` enforces it — `EvalChecker.java:106-115`) | N |
| D49 | S2 | Cross-store joins are not a type-level concept and the store boundary is enforced only as a side effect of connection routing: two logically distinct Databases sharing one connection are joined silently into a single unqualified SQL join. | No store identity in the join schema rule; the only wall is `NotImplementedException: query touches stores bound to DIFFERENT connections` at execution | A15 (`#>{xs::DbA.T_A}#->join(#>{xs::DbB.T_B}#, …)` types clean and runs when the connections coincide, carrying D31's `[1]`-NULL defect on top) | N |

| D81 | S2 | **The native catalog diverges from real FINOS Legend in 183 of its 721 signatures (25.4 %)**, and 5 of those divergences are exactly the "narrow the return multiplicity" mistake that licenses the `[1]`-NULL family. | `builtin/Pure.java` (721 `signature("…")` sites) diffed against `finos/legend-pure@18cd1bb` + `finos/legend-engine@ef037ea` (24 172 real non-test declarations, both checkouts on disk) | **A30 — the independent oracle V8 said did not exist.** EXACT 538 (74.6 %), RET_DIFF **16**, ARG_DIFF **131**, NO_SUCH_FUNCTION **36**; plus **246** real functions missing from the 11 families lite claims to support (1031 across all 51 packages). Return-multiplicity narrowings, each executed to `null` under a `[1]` column: `percentile`, `corr`, `covarPopulation`, `covarSample` (`[0..1]`→`[1]`), and `stdDev`/`variance` (real requires a mandatory `isBiasCorrected` and `Number[1..*]`; lite drops both and keeps `[1]`). Other high-consequence: `datePart` narrows `Date[1]`→`StrictDate[1]`; `relation::extend` redeclared over `C[*]` (schema erased, and `[1,2,3]->extend(~b:x\|$x+1)` type-checks then ICEs); `relation::over` drops `⊆T` everywhere so a window on a non-existent column is no longer a type error; `relation::sort(Relation<T>[1],String[*])` is invented with no column-membership check; `lang::compare(Any[1],Any[1])` admits String-vs-Date; `math::abs<T>` replaces four monomorphic overloads (→ D36); `collection::first(T[*],Integer[1])` is invented (→ D07); **21 of the 36 nonexistent functions are demonstrably callable from user Pure** | supersedes **V8** |
| D82 | S2 | The catalog is not even the compiler's own single source of truth: at least 8 further function names are type-checked by literal string desugars in `Typer` with **no** catalog entry, so auditing the 721 signatures does not audit the public function surface. | `compiler/spec/Typer.java:607, 615, 636, 643, 652, 657, 672, 681, 694` (`tdsVocab`) | A30 (`union`, `paginated`, `renameColumns`, `renameColumn`, `restrict`, `restrictDistinct`, `columnValues`, `olapGroupBy`, `projectWithColumnSubset`, `window`, `func`, `pathWithAlias` — all probed and typed; nothing pins their argument or result shapes against anything) | N |
| D83 | S2 | `executePlan`'s decline tunnel catches `RuntimeException` and re-executes the query bare — so it will swallow the egress's own soundness walls, and `SeedableLets` swallows every `RuntimeException` from a trial lowering on every `execute()`. | `StatementExecutor.java:2716-2785` (catches at `:2719`, `:2759`, `:2774`); `lowering/SeedableLets.java:37-50`, reached at `StatementExecutor.java:2325-2327` | A25 (the `RuntimeException` arm covers `Executor.java:356` "NULL cell reached COLLECTION egress" and its siblings — the only runtime guards D31 has left). Together with D68 this means the two instruments that could detect the top defects are both disabled | N |
| D84 | S2 | `effectMemo` breaks recursion cycles by writing `false`, so a transitively EFFECTFUL function is classified PURE for the rest of the body. | `StatementExecutor.java:113` (one memo per body) + `containsEffect:2202-2263` (`memo.put(key, false)` for in-progress) | A25 (analysis + the recursion guard is separately RESET on the `execute()` runtime-argument path, `:2178-2183`) | N |
| D85 | S2 | Type-valued fallbacks that guess a type the model never supplied: a project column's element type falls back to `String`, a zone-suffixed TDS timestamp is typed the ABSTRACT `Date` and then ICEs, a `@Relation<(c:T)>` column with no multiplicity silently becomes `[1]`, and `PctProbe` types every non-DATE/non-TIMESTAMP result column `VARCHAR`. | `lowering/Lowerer.java:1409-1414`; `compiler/spec/TdsChecker.java:239`; `compiler/spec/Typer.java:3100-3103`; `exec/PctProbe.java:41-49` | A21 (every type-valued fallback site in the repo hand-reviewed: **10 VIOLATIONS**, 24 suspicious-but-gated, 20 legitimate); A25 (`PctProbe` decides the SQL type of deferred-TDS/pivot columns at the execution boundary) | N |

| D94 | S2 | `SqlType.Struct.Field` carries neither nullability nor a bound, so a `[1]` class property is representationally identical to `[0..1]` and `[2..5]`/`[1..*]` to `[*]`; two supertypes declaring the same property at different multiplicities give `findProperty` = `[1]` and the layout `[*]`, and the runtime value is a **list** under an `Integer[1]` stamp. | `sql/SqlType.java` (`Struct.Field` has no nullable/bound component); `compiler/element/ClassLayouts.java` vs `PureModelContext.findProperty:186-208` | A28 (`^mi::Req()` — a class with a `[1]` and a `[0..1]` property — is representationally one struct; `^model::DiamondMult(w=1)->map(d\|$d.w)` returns a list under `Integer[1]`) | N |
| D95 | S2 | The lineage and validation surfaces re-derive structure without the typed HIR and get it wrong: `ScanColumns` reports **no** lineage at all for a pivot, lineage is a flat set of physical columns with no per-output edges and no types, `PkInference` dispatches on a function's **simple name** so a user function named `sort`/`filter`/`select` is silently treated as the built-in, and `ValidateDesugar` duplicates a diamond-inherited constraint, never checks the `~message` column's type, silently mis-binds its runtime argument and defaults a missing enforcement level to the literal `"Error"`. | `lineage/ScanColumns.java`, `lineage/ScanRelations.java` (`rootImpl` picks the FIRST class mapping for a multi-set class; `rootFor` swallows every `RuntimeException`), `lineage/PkInference.java`, `validation/ValidateDesugar.java`; `plan/InProtocol.java` hardcodes `VARCHAR(1024)` and `type = String` for **every** temp-table collection | A29 (each item run; the `MESSAGE:String` contract column comes back `Integer`; `validate(...)` with no `.all()` root is a raw `IllegalStateException`) | N |

| D103 | S2 | `spi/ElementSink.accept` validates nothing, so a third-party grammar can inject duplicate, malformed and **null** FQNs and `compileModel` dies with a raw NPE; and the SPI registry and the lexer desync, so an overlay can shadow a lexable built-in and the parser silently ignores it. | `spi/ElementSink.java` (no validation on accept); the SPI registry vs `lexer/Lexer` keyword set | A31-F17, A31-F18. The extension seam has no type or name contract at all | N |

### S3 — MEDIUM: an internal exception escaping on plausible input, or a construct that type-checks and cannot execute

| ID | Sev | One-line claim | Root site (file:line) | Manifestations | Orch-verified |
|---|---|---|---|---|---|
| D50 | S3 | Raw JDK exceptions escape as ICEs from record compact constructors, literal validation and index reads, on ordinary model or query text. | `Type.java:160-167` (`DECIMAL(2,5)` — **V1-falsifier corrected the phase**: `compileModel` *succeeds*, the IAE fires lazily at **Phase G** from a table *reference* via `StoreCompiler.columnType:192` ← `PureModelContext.resolveTableWithIncludes:436` ← `TableReferenceChecker.check:73`, so a model with a bad DECIMAL passes validation and explodes only when someone queries that table), `:520-534` (duplicate relation columns), `Multiplicity.java:145-155` (`[5..2]`) reached from `StoreCompiler.java:192`, `TypeClassifier.java:111-122`, `Typer.relationShapeType:3103-3106`, `ModelIntegrity.checkFunction:167`; `Typer.java:1084-1089` (`at(99)`); `protocol/spec/CTime.java:35-41` (`%25:00:00`); `parser/SpecParser.java:825-836` (`1e309`); `TokenStreamCursor.java:794-797` (type-variable value overflow) | A01, A08, A13, A17-F9/F11/F12, A22, A05-8. The **same rule** on the colspec path is a clean `SchemaInvariantException`, and the parallel date path is a clean `ParseException` — two implementations, one clean, one an ICE | N |
| D51 | S3 | `StrictTime` and `Byte` are first-class Pure primitives with **no SQL carrier**: any query touching one — or merely a `VARBINARY` column *declared* in a Database and never referenced — dies with a raw `IllegalStateException`/`NotImplementedException` after Phase G accepted it. | `lowering/PureSql.java:91-104` (`case BYTE, STRICT_TIME -> null` then throw); the scalar default arm for `TypedCTime` | V6; A08; A16 (`cast(@StrictTime)`); A19 (an unmapped `BLOBBY VARBINARY(10)` kills every plan over the table); A10; A22; A14 (no relational column can carry `StrictTime` at all) | **Y — V6** |
| D52 | S3 | A zero-column relation type is accepted, lowers to `SELECT *`, and ICEs at the egress with a plan/schema mismatch — leaking unmapped physical columns on the way. | `ProjectChecker` has no `~[]` guard (`SelectChecker.java:23-25` and `DistinctChecker.java:42-44` both *do*); `Lowerer.relationCast:3208-3255` (`allKnown` is vacuously true for an empty target) | V15; A13 (3 shapes); A09-F11 (identical on all three backends); A17-F7 (`cast(@Relation<()>)`) | **Y — V15** |
| D53 | S3 | The advertised surface is much larger than the lowerable surface: 87 natives type-check and then raise `IllegalStateException: no scalar lowering registered`, 8 `TypedSpec` variants reachable from plain queries have no lowering arm, and the reduce position accepts natives with no aggregate lowering. | `lowering/Scalars.RULES` coverage vs `builtin/Pure.all()`; `lowering/Aggregates.reducerFor:139-146`; `Lowerer` dispatch vs the 70 permitted `TypedSpec` variants | A06 (exhaustive: **all 721** overloads typed at G — 523 OK, 198 clean errors, **0 internal**; of the 523 planned, **112 raise internal exceptions**: 87 "no scalar lowering", 19 `NotImplementedException`, 6 other); A22 (`TypedCTime`, collection `TypedSortBy`, class `TypedCast`, `TypedColSpec(Array)`, `TypedSortInfo`, `TypedOver`, `TypedTypeRef`); A14 (`last`, `toOne`, `makeString`, `at`, `fold` in reduce position) | N |
| D54 | S3 | `StackOverflowError` escapes from six distinct frames — parser, name resolver, typer, store resolver, lowering — on deeply nested but legal input, and being an `Error` it bypasses every `catch (Exception)`. | No depth budget anywhere: `TokenStreamCursor.parseType:725/759`, `SpecParser.parseUnaryAndPrimary:587`, `NameResolver.resolveVs:1481`, `Typer.synth:126`, `SyntheticHeads.liftFilteredHeads:298`, `Lowerer.scalarInner:2350` | A17-F13 (**thresholds withdrawn** — V1-falsifier got OK at generic depth 1200 and paren depth 800 where A17 reported SOE at ~981/~727; the numbers are properties of the auditor's JVM stack, not of the parser. The SOE itself reproduces at depth 5000, at `TokenStreamCursor.parseQualifiedName:535`); A20 (320 `+` terms, 200 nested parens, 400 nested `if`, 500 nested concat). The repo's own NLQ module generates queries | N |
| D55 | S3 | Bad SQL from a fully type-checked plan — seven distinct shapes, one of which kills the database session. | `sql/dialect/AnsiSqlRenderer.java:115-121` (set branches written inline, never parenthesised); `Lowerer` groupBy+size fusion; constant `extend` folded into `GROUP BY`; `Lowerer.java:606-611` (negative LIMIT); `drop(0)` → `OFFSET 0`; empty column name → zero-length identifier; unguarded `round()`/`bitShiftLeft`/INT32 arithmetic | A20 (**431 of 12 800** generated queries; `UNION` after `sort`/`limit`/`drop` alone is **234**; `drop(0)` triggers a DuckDB assertion that invalidates the connection for 587 subsequent queries); A13 (`slice(2,0)` → `LIMIT -2`; `AS ""`); A06-10; `GROUP BY <literal>` is a **silent wrong-answer** hazard when the constant is a valid ordinal | N |
| D56 | S3 | `IllegalStateException: a scalar query has no row scope` — count rows, filter the count, ask how many are left. | `lowering/Lowerer.java:326-329` | A20 (seed 5200047, shrunk by hand to `groupBy(~[], ~[c8:…count()])->filter(v11\|$v11.c8 < 1)->size()`). This is the repo's own "genuine internal invariant violation" bucket escaping to the user | N |
| D57 | S3 | `instanceOf` ignores the subtype relation entirely and raises `NotImplementedException` on **every** non-identical pair, including statically decidable ones; the function can only ever return literal `true`. | `lowering/Scalars.java:2563-2585` (`ctx.isSubtype` is never consulted) | A03 (3 repros incl. `instanceOf(1, Integer)`; the `@Type` spelling is separately rejected at G for every type) | N |
| D58 | S3 | lite-INTERNAL vocabulary is reachable from user query text by FQN and then throws `NotImplementedException` (a plain `RuntimeException`, not a `LegendCompileException`). | `compiler/spec/CoreFn.java:159-181` applies the `INTERNAL_DESUGAR` guard on the **bare-name branch only**; `JoinChecker.java:47-48` computes the slot spelling from the FQN before canonicalizing | A15 (`meta::legend::lite::join`, `meta::legend::lite::legacyNavigate`; the bare spellings are correctly refused) | N |
| D59 | S3 | An empty collection `[]` as an argument makes 27 native overload sets ambiguous, and the resulting diagnostic names the wrong function. | `compiler/spec/InferenceKernel.java:1061-1067` (`if (isNil(actual)) return 0;`) → tie → `:936-953` throws; `:888` names `candidates.get(0)` | A07 (exhaustive simulation: **998 498** resolutions over a 72-point grid, **763 ambiguities, every one involving `Nil`**; `[]->max()` blames `date::max` for a tie among `math::` overloads) | N |
| D60 | S3 | Type-checked functions and aggregates with no spelling on the connected dialect surface as raw JDBC/dialect exceptions; there is no capability check before rendering. | No pre-render capability gate for the scalar or aggregate lane; `Compiler.java:561-564` gives SQLite DuckDB's vocabulary | A11-F17 (11 ordinary expressions fail on SQLite; ~half a normal query surface); A14 (H2: `ANY_VALUE` not found, `hashCode` `DialectCapability`; SQLite: 8 aggregates missing); A11-F13/F14 (`toSQLString` raises a bare `UnsupportedOperationException` and an `IllegalStateException` where a `DialectCapability` is specified) | N |
| D61 | S3 | The DB-built JSON/CSV wire is not readable by the platform's own readers: non-finite Floats emit bare `Infinity`/`NaN`, and an integer token wider than a `long` overflows `Long.parseLong` — both break `sql/Json.parse`, `server/Json.parse`, `LiteralText.parse` and the HTTP `/query` endpoint. | `sql/Json.java:171-186` (`num` scans only `+-0123456789.eE`, then `Long.parseLong`); `values/LiteralText.java:234-240`; the float repr in `AnsiSqlRenderer` | A09-F8/F9/F10 (37 literal spellings swept; every failure listed); A19 (`REPARSE_FAIL` on the platform's own output). H2 refuses the whole statement instead | N |
| D62 | S3 | `resolver/AsorRef`'s `objectReference` builds JSON by raw string concatenation with no escaping — a `"` in a primary key yields invalid JSON, a `\` crashes the whole query. | `lowering/SnapshotEnvelope.java:100-116` (`"\"pk$_" + i + "\":" + (strPk ? "\"" : "")` around an un-escaped cast) | A19 (both repros, with the base64-decoded reference pasted). Same site embeds a hard-coded **H2** connection descriptor for a DuckDB runtime | N |
| D63 | S3 | `ArithmeticException: Rounding necessary` escapes from a union over an Integer-declared DECIMAL column, and raw `IndexOutOfBoundsException` escapes from three other surfaces. | `UnionSynthesis` emits no cast because `typeAsDeclared` already claims Integer, then `exec/Executor.java:659` calls `toBigIntegerExact()`; `exec/MetamodelSteps.java:55-62` (`->at(n)`, no bounds check); `compiler/spec/TdsChecker.java:82-91` (long TDS data row) | A18; A19 (the ordinary `at()` lowering emits a clean Pure-worded error — two implementations of one rule); A14 | N |

| D86 | S3 | The MIR carries a free-form SQL **function-name channel** disguised as a `StringLit` in argument 0, read back by four dialects with `switch (String)` and spliced verbatim, unquoted and unvalidated — with 7 unchecked `(SqlExpr.StringLit)` casts that are `ClassCastException`s on a wrong arg-0 shape, and a silent wrong emission on DuckDB where H2 walls loudly. | Producer `lowering/DateShifts.java:65-79` (`static String intervalFn(String unitName)` — the banned `mapXxxName`); consumer `sql/dialect/AnsiSqlRenderer.java:633-643`; re-parsers `H2.java:212-224`, `EngineStyleH2.java:1411-1425, 1475-1478`, `EngineStyleDB2.java:209-224`; casts at `AnsiSqlRenderer.java:634, 640, 642`, `EngineStyleH2.java:1483, 1488`, `H2.java:79, 86` | A23 (probe: `ADD_INTERVAL StringLit("1) ; DROP TABLE t --")` renders verbatim; an unknown unit is silently emitted on DuckDB and a `DialectCapability` on H2). **Not an injection vector today** — `intervalFn` throws on an unknown `DurationUnit` — but a typed `enum IntervalUnit` would make every site a javac error | N |

| D96 | S3 | Error discipline **degrades monotonically** down the pipeline: 850 of 1 168 `throw new` sites are internal (72 %), rising from 25 % in the parser to **100 %** in `sql/`, `sql/dialect/`, `exec/`, `plan/`, `values/` and `protocol/`; and `NotImplementedException` — the most-thrown type, 293 sites — extends `RuntimeException`, not `LegendCompileException`. | Orchestrator census over `core/src/main/java`; `error/NotImplementedException.java:8` | V37 (per-package table). A fleet sweep of 247 queries over the whole native surface produced **85 internal exceptions vs 5 user-facing errors**, every one of which had passed phase G. This is the mechanism behind D53: type-checks-then-cannot-execute is the *default* failure mode of the back half, not an edge case | **Y — V37** |
| D97 | S3 | `planToString` / plan-text has four reachable holes that are internal errors on a plain query: `PlanText.pureName:567` has no `PrecisionDecimal`, `LatestDate` or `StrictTime` arm, a computed TDS column over two source columns is an internal error, and an in-protocol constant of any non-String/non-Integer collection type walls. | `plan/PlanText.java:567`; `plan/InProtocol.java` | A29 (`planToString(executionPlan({\|let p = 1.5d; …}))` → `NotImplementedException: plan: pure type name for PrecisionDecimal[38,1]`; the same query with `1.5` renders fine; `%latest` and `%12:30:00` likewise). **A29 REFUTED the forwarded stronger claim** that `:557` throws for any DECIMAL *store column* — see §6a | N |
| D98 | S3 | Synthesized names collide with user names with no reservation or check: a user property named `pk_0` collides with the synthesized primary-key column, and `DriverPkAppend` collides with a user projection column named like the driver PK. | `resolver/Substitution.java`, `resolver/DriverPkAppend` (no namespace reservation for synthesized column names) | A32 (findings 8, 9) — the same class of defect as D19 (`$` synth FQNs) and D26 (`u_map__` prefix), i.e. **three** synthesized namespaces in the compiler are user-writable | N |

| D105 | S3 | The public HTTP/API boundary turns every failure into an untyped, phase-less leak: **127 of 863** probes across the public API escape as raw internal exceptions, the error envelope drops `Phase` entirely and returns **HTTP 200 for every failure**, it leaks internal record dumps, internal FQNs, rendered SQL and DB catalog internals, and a `StackOverflowError` on `/engine/execute` closes the connection with **zero bytes** while the same input on `/engine/diagram` returns `{"error":""}`. | `server/LegendHttpServer` (the error envelope), `error/LegendCompileException.Phase` (RENDER/EXECUTE unused) | A31-F2/F4/F9. The information-leak half is the one item in this audit with a security flavour and should be triaged separately | N |

### S4 — LOW: inconsistency, dead code, information loss with no observed wrong value

| ID | Sev | One-line claim | Root site (file:line) | Manifestations | Orch-verified |
|---|---|---|---|---|---|
| D64 | S4 | Column identity and "trust the name" are implemented inconsistently across the operator set: `rename` on a quote-bearing column is a complete **no-op**, UNION uses quote-insensitive `sameColumn` while DIFFERENCE uses raw equality, `project` trusts every name on a late-bound grid while every other operator trusts none, `__|__` is accepted as an ordinary user column name, and `presentPivotName` is not injective. | `RenameChecker.java:71-81` vs `InferenceKernel.java:442-447`; `InferenceKernel.java:819` vs `:833-838`; `Typer.java:2922-2937` vs `InferenceKernel.unifyConstraint:307-325`; `Type.java:470-475` | A04 (rename no-op proven; the same relation raises correctly for `extend`); A13 (9-operator late-bound table; `distinct()`'s message is factually wrong); A01 (two distinct header spellings collapse onto one; the TDS-born column becomes unaddressable from `filter`); A06-11 | N |
| D65 | S4 | One typing rule, many hand-rolled copies that disagree: **6** live spellings of "is this stamp many/to-one?" (disagreeing on `[0..0]` and on `Var`, 53 ad-hoc `upper()` pokes), **6** independent primitive-subtype ladders (2 omit `LatestDate <: Date`), **3** implementations of the `__|__` rule, **2** different LUBs for the same pair, and `typeName()` is not injective yet is used for comparison and keying at 4+ sites. | `Multiplicity.java:35` vs `Stamps.java:37/47/54` vs `StatementExecutor.java:1757` vs `CanonicalRenderSql.java:258` vs `StampCensus.java:63` vs `ResultShape.java:73`; `ModelContext.java:233`/`InferenceKernel.java:1386`/`:565`/`:361`/`CastPolicy.java:216`/`MatchFold.java:41`; `Type.java:477-505` vs `Fold.java:525-530` vs `:867-882`; `InferenceKernel.commonSupertype:1238` vs `valueLub:554`; `Type.java:116-120, 261-274, 307-314, 545-550` | A02 (transcribed truth table; the `[0..0]` hole is what turns D32 into a SCALAR shape for a 3-row result); A03; A01; A04. The `valueLub` split is observable: the same two temporal values are `Date` through a collection literal and `Any` through `concatenate`, changing how the wire values are **decoded** | N |
| D66 | S4 | Declared type/multiplicity metadata is dropped at phase boundaries: `exec.Column.multiplicity` is `null` at 4 of 7 construction sites and has **zero** read sites repo-wide; `NameResolver` drops `Generic.multiplicityArguments`, `typeVariableValues` and every `pos`, and flips `Column.multiplicityDeclared` to false; a `+` local property's declared type and multiplicity are thrown away; `Function.signatureKey()` embeds `SourceInfo` so the duplicate-signature guard is defeated by source position; protocol `long` bounds are narrowed with a raw `(int)` cast. | `exec/Column.java:18-22`; `Executor.java:766`, `ExecutionResult.java:84/107/144`; `NameResolver.java:452-457`, `:486-491`; `MappingNormalizer.java:2632-2636` + `NewChecker.java:75-82`; `model/Function.java:51-65`; `model/MappingFromProtocol.java:605-610` | A02, A09-F17, A14 (pivot columns all `mult=null`); A17-F14/F15/F18 (`Varchar(200)` loses its `200`; two identical declarations on different lines both compile); A18 | N |
| D67 | S4 | The typed HIR does not defend itself and the traversal spine is incomplete: **0 of 70** variants validate `info != null` or any coherence with their children, malformed nodes reach the rendered SQL *and* the plan's declared output contract while disagreeing with each other, and `children()` omits 7 slots that the store-escapee post-condition is nominally enforced over. | `compiler/spec/typed/*.java` (30/70 have a compact constructor, all defensive copies bar 2); `TypedAggColSpec.java:19-21`, `TypedAggColSpecArray.java:21-27`, `TypedExtendAgg.java:23-31`, `TypedExtendWindow.java:31-41`, `TypedPivot.java:35-44`, `TypedGraphFetch.java:35-37`, `TypedSerialize.java:25-30`; `resolver/StoreEscapees.java:22-40` | A22 (11 hand-built malformed nodes: 8 reach the DB, 3 are caught — by the *lowerer*, not the HIR); A12-H7 (all 70 variants × 101 spec-carrying slots probed; `resolve()` returns OK with a live `TypedGetAll` in a `.orderKey` slot). Both auditors state honestly that neither is reachable from surface syntax today | N |
| D68 | S4 | `SqlTyping.carryThrough` is exactly the "forgiveness by kind pair" the file's own contract says no longer exists, and once a column is tagged `tolerated` the label/wire tripwire is permanently muted; `SqlTypeCensus.MISMATCH` is structurally unreachable because reconciliation runs first. | `sql/SqlTyping.java:238-244` and `:145-155`; tag stamped at `Scalars.java:674-676` and `Lowerer.java:1391-1405`; `SqlSelect.java:23-33` reconciles before `SqlTypeCensus.walk:442-521` compares | A08 (both of its UNSOUND findings ride this arm and both report `diverge=0`; a hand-built select declaring BIGINT over an INTEGER projection comes out of the constructor already rewritten). This is the instrument that *should* have caught D01 and D41 | N |
| D69 | S4 | Wire information loss with no recoverable signal: CSV cannot distinguish SQL NULL from the empty string, CSV truncates DateTime sub-seconds, one DateTime value has **four** lexical forms across exits (two not re-parseable by the platform's own reader), the abstract `Date` wire form is chosen at RUNTIME by a `typeof()` test in the emitted SQL, and written temporal precision survives only at the query ROOT. | `lowering/Render.java:731-738`, `:725-728`; `sql/DateFmt.java:57-60` (`CSV_DATETIME` has no sub-second part); `lowering/Fold.java:961-975`; `values/PureDateLiteral.java:223-234`; `lowering/RootLiterals.swap` is called from exactly one site, `Lowerer.java:323` | A19 (byte-level CSV proof; `Dt.java` round-trip table); A09-F12/F15; A08 (`%2020-01-01T10:00:00.000` as a cell loses `.000` while `->toString()` of the same literal keeps it, in the same query) | N |
| D70 | S4 | Configuration and modality flags are silently ignored: `alloyConfig`'s `dateTimeFormat` is discarded and any non-literal config argument reads as all-defaults; `removePropertiesWithNullValues` cannot remove the first property's null (and does nothing for a one-leaf tree); `allVersions()` on a non-temporal class silently returns the plain extent while `all(%date)` walls; `compileModel` defers user-model errors as "poisons" reported later as something else; `let` re-binding with a different type is accepted; runtime/connection elements are hardcoded `Any[1]` so `from(runtime, mapping)` and `from(mapping, runtime)` are indistinguishable at G. | `resolver/GraphEmission.java:3176-3195`, `:3241-3249`; `lowering/SnapshotEnvelope.java:58-80`; `GetAllChecker.checkVersions:35-42`; `MappingNormalizer.java:313-341, 398-403, 420-440, 1671-1675`; `SpecCompiler.typeQueryBody:185-203`; `Typer.java:2387-2391` + `FromChecker.java:83-93` | A19 (byte-identical SQL for two different `alloyConfig` calls); A15; A18 (`compileModel SUCCEEDED` on two broken models); A16; A05-16 | N |

| D87 | S4 | MIR construction-time type facts contradict the wire the MIR's own dialect emits: `DATE_TRUNC_DAY` is typed `TIMESTAMP` but every dialect emits a `DATE`, `OrderedListAgg` is unconditionally `VARCHAR` but emits a LIST, `EPOCH_SECONDS` is typed `BIGINT` but `epoch()` returns `DOUBLE` (and has no producer), and `IntLit`/`ArrayLit` claim `BIGINT`/`Array(BIGINT)` where DuckDB delivers `INTEGER`. | `sql/SqlTyping.java:389-397`, `:329`; `sql/SqlExpr.java:546-550`; `sql/AnsiSqlRenderer.java:624, 410-411, 390` | A23 (`FactWire.java` — MIR fact vs the DuckDB type of the dialect's *own* emission). The Pure-level decode is driver-kind-keyed and currently recovers, so these are INCONSISTENCY not unsoundness — but they are the same class as the `subsumes(TIMESTAMP, DATE)` arm that hides them | N |
| D88 | S4 | Two further MIR/label losses and a weak invariant: `SqlRewriter` drops `SqlExpr.Lambda`'s stored `TypeFact` where `withChildren` preserves it (harmless only because **no producer ever populates it** — all 39 construction sites use the 2-arg ctor), `reconcileLabels` drops `OutputCol.tolerated` on the ADOPT path where `reconcileUnionLabels` preserves it, `SqlAgg.Reducer` accepts all 41 `Fn` constants (16 of which yield `UNKNOWN`) despite the "position typing" claim, and a null-outputs `SqlSelect` is constructible. | `sql/SqlRewriter.java:245-248` vs `sql/SqlExpr.java:184-186`; `sql/SqlTyping.java:163` vs `:213-215`; `sql/SqlAgg.java:5-12`; `sql/SqlTyping.java:71-73` + `SqlSelect.java:22-33` + `SqlQuery.java:14` | A23, A08. All four are latent: no production site reaches them today | N |
| D89 | S4 | The wire census disagrees with the renderer it is censusing: `SqlTypeCensus.normalizeMeta` has no arm for `DOUBLE PRECISION` — which is exactly what `TypeNames.H2` emits and what H2 reports back — so every H2 `DOUBLE` and every H2 `DECIMAL` read is counted as a wire DIVERGENCE, and the census additionally swallows `RuntimeException` into a `probe-error` counter. | `sql/TypeNames.java:54` vs `exec/SqlTypeCensus.java:400-418` and `:355-367`; catch at `:210` | A27 (two false-positive divergences from an ordinary four-column H2 read). Combined with D68 (the census's structural blindness to real mismatches) the instrument is wrong in **both** directions | N |
| D90 | S4 | `execute()` lowers with `withEngineExistsJoinForm` **unconditionally** while `plan`, `planStreaming`, `executeStreaming`, `executeWire` and `lowerResolved` all gate it on `!temporalRoot`; and `execute(...)->size()` folds to `1` unconditionally where the let-bound spelling folds only when relation-rooted. | `StatementExecutor.java:2316-2319` vs `Compiler.java:353-363` and `:654-666`; `compiler/spec/ResultEnvelopeSplice.java:207-215` vs `:216-234` | A25 (two more ways the executing surface and the inspectable surface are not the same pipeline — see D47) | N |

| D99 | S4 (META) | **The 4 278-test suite is green and structurally cannot see any of this.** Of 1 671 test methods that execute a query end to end, 888 (53.1 %) assert row values, **4 (0.24 %)** assert a result column's `pureType()`/`multiplicity()`, and **exactly 1 (0.06 %)** relates a declared column type to the *delivered Java carrier*. Compile-time typing is densely and adversarially tested; row values are densely tested; the seam between them is unguarded — and that is exactly where the audit's findings sit. | `core/src/test` (7 `Column.pureType()` assertion lines vs 1 943 `.rows()` sites vs 87 `"SELECT` literals) | V36 + A37: full run = **484 test classes, 4 278 tests, 0 failures, 0 errors, 16 skipped**. Of 19 findings put to a coverage check: **0 COVERED · 3 PINNED · 15 UNCOVERED**. Three are *pinned*, i.e. an existing test asserts the current wrong behaviour and fixing the bug turns the suite red: V3 (`cast` converts — pinned hard), V11 (`EqualityWorldsConformanceTest.declaredDivergences:90` pins `1 == 1.0 → true` and `8 == 8D → true` as accepted), V13 (`PrecisionDecimalArithmeticTest` has 9 tests incl. a 20×20×20×20 sweep on code whose only caller is that test file — deleting the dead code turns 9 tests red) | **Y — V36** |

| D104 | S4 | The public API exposes **no usable type information**: a GRAPH result (a bare `Class.all()` — the commonest query shape) carries no per-field type on any surface and `executeWire` reports `columns = []`; the CSV wire collapses NULL≡empty-string, Integer≡String-of-digits, Boolean≡String and temporal≡String; the JSON wire makes `Decimal` and `Float` byte-identical; the IDE/LSP surface has no hover types and no completions and reports diagnostics only for `PARSE`; `QueryService.executeSql` runs the SQL and **discards the ResultSet**, returning a zero-column relation type; and `DiagramService` invents an inheritance edge the compiler does not have and collapses distinct types to simple names. | `server/QueryService`, `server/LegendHttpServer`, `ide/*`, `server/DiagramService`; `Phase.RENDER` and `Phase.EXECUTE` have **zero** throw sites, so no execution failure can ever carry a phase | A31-F3/F5/F6/F7/F10/F11/F12/F13/F14/F15/F19/F20. Scored S4 because no wrong *value* is produced — but this is the surface a real consumer actually meets, so the practical impact is higher than the score | N |

### S5 — DOC: a prose claim contradicted by the code

| ID | Sev | One-line claim | Root site (file:line) | Manifestations | Orch-verified |
|---|---|---|---|---|---|
| D71 | S5 | `native-catalog.txt` is a **SELF-golden**: `NativeFunctionTest.catalogMatchesTheGoldenFile` renders `Pure.all()` and compares it to a file generated from `Pure.all()` — and its renderer additionally erases the two forms that matter, so **flipping a relation column from `[1]` to `[0..1]` inside any signature produces a zero-line diff in the only catalog-wide guard**. | `builtin/Pure.java:14-19` (the "VERBATIM … NO divergence categories remain" header); `NativeFunctionTest.java:52-66` and `renderType:90-107`; `Corpus.java:48-51, 68` (the one upstream-touching mechanism is opt-in and absent on this machine) | V8; A30 (golden-vs-code diff = **0 lines** for all 721; three signature pairs differing only in `Result<X|1>` vs `Result<X|*>` or a relation column's multiplicity render **identically**; the other 30 tests in the file restate the same data or check internal closure). **Now measured rather than suspected — see D81.** | **Y — V8** |
| D72 | S5 | `Type.java`'s three headline claims are false: "callers never normalize `PrecisionDecimal → DECIMAL`" (**7 sites forget it, not 4** — V1-falsifier read all 28 `Primitive.DECIMAL` references: no arm at `PlanText:557`, `Repr:51`, `DateCtorRule:100`, `Scalars:3467`, `CastPolicy:144`, **`CanonicalRenderSql:224`**, **`Typer:1815`**; 14 carry it. A01's census is the accurate one; **V14's list of four is incomplete**), "THE PIVOT-COLUMN MATCHING RULE (one owner)" (three disagreeing implementations), and "`MAX_PRECISION` — widest compatible precision" (the constructor accepts `DECIMAL(2000000000,0)`). | `Type.java:150-156`, `:455-463`, `:177-178` | V14 (with the observable consequence: `format('%r', 1.25D)` and `format('%r', 1.25)` are indistinguishable because `lowering/Repr.java:51` lacks the arm); A01; A03. The seven missing arms: `PlanText.java:557`, `Repr.java:51`, `DateCtorRule.java:100`, `Scalars.java:3467`, `CastPolicy.java:144`, `CanonicalRenderSql.java:224`, `Typer.java:1815` | **Y — V14** |
| D73 | S5 | `AGENTS.md` §3a "The MIR is closed and pure data" is wrong on four counts (`SqlExpr` is 36 variants not 32; `SqlAgg.Fn` is 41 not ~35; `SqlType.Struct` is omitted; `SqlExpr.Cast` carries a `SqlType`, not a Pure type name), and the "no MIR record has a String field encoding a SQL operation" invariant is violated at 6 sites — while the Lowerer itself holds DuckDB function names. | `AGENTS.md:205-232`; `SqlSource.java:148-172`, `SqlExpr` interval units, `PlanParam.enumMapFn`, `SqlSource.RawSql.sql`, the `ADD_INTERVAL`/`TIME_BUCKET`/`EXTRACT` positional convention; `lowering/DateShifts.java:63-75`, `CalendarAgg.java:197`, `Scalars.java:657-663, 3065` | A08 (reflective variant enumeration + the renderer sites that splice each string); A10 (`CanonicalRenderSql.java:519-522` compares DuckDB `json_type()` output strings) | N |
| D74 | S5 | The repo-wide rule "**NO FALLBACKS. NO DEFAULTING.**" is falsified at scale. | Census over `core/src/main/java` | A21 (4 237 rows): 572 `null_ternary`, 442 `default ->`, 409 `Type.Primitive.*` stand-ins, 259 `notnull_ternary`, 210 `instanceof_else_default`, 186 `orElse`, 151 `isEmpty_ternary`, 52 `getOrDefault`, **25 swallowed catches**, 14 swallowed-empty, 36 fallback-returns; plus 383 `NotImplementedException` and 283 `IllegalStateException` throw sites. A18's own package census (10 catches: 5 silent; 41 `orElse(null)`) ranks the worst offenders: `RelOpTranslator:110` > `MappingNormalizer:2400/2380/2476` > `:326` > `:1673` > `:3035` | N |
| D75 | S5 | Roughly twenty further javadoc/comment claims are contradicted by their own code. Ranked below every real defect; each is the *documentation* half of a defect already listed above. | Non-exhaustive: `UserCallInliner.java:45-48, 103-104` (α-hygiene "unconditional", "callee bodies are closed") → D05; `TypedMatchRuntime.java:20-21` ("SQL lowering walls LOUDLY") → D09; `StoreResolver.java:1073-1077` ("fails loud — never silent") → D31f; `MappingNormalizer.java:3333-3336` ("the residual null-check is toOne's runtime semantics") → D31a; `AnsiSqlRenderer.java:479-483` ("`to_json` yields the same array value") → D14; `GraphEmission.java:3163-3166` ("every OTHER flag walls loudly") → D70; `TypeNames.java:12-18` ("absence is LOUD") → D15/D47; `ModelIntegrity.java:18-25` + `PureModelContext.java:71-74` ("**every** reference … is checked") → D38; `SynthFqn.java:17-19` ("`$` is non-user-writable") → D19; `NormalizeFolds.java:17-18` ("applied at TYPING") → D06; `AsOfJoinChecker.java:19-26` + `TypedAsOfJoin.java:20-22` + `Checkers.java:44-46` ("prefixes EVERY right column") → measured: only the colliding ones; `PlatformTypes.java:9-13` (drift test "pins its constants") → 6 of ~45 pinned; `JoinChecker.java:45-47` ("a bare 'join' must never reach it") → D58; `Typer.java:70-82` ("owns exactly two things"); `ValueSpecification.java:16-30` ("3 variants") → 24; `PureDateLiteral.java:19-23, 57-62` (grammar + "byte-exact round-trip") → D69; `Lexicon.java:32-41` + `CarrierStrategies.java:31-42` (SQLite gaps understated; no `Caps.SQLITE` exists) → D15; `TypedFilter/Limit/Drop/Slice` ("info() is the source type unchanged") → 8 violations in 112 queries; `Multiplicity.java:22-33` + `Stamps.java:9-18` ("THE single implementation", "one principle, one reader") → D65; `MappingNormalizer.java:2285-2291` (Boolean coercion "ERRORS … loud beats silently-different") → it does not error and returns the **opposite** of the engine value it cites; `Type.PrecisionDecimal` javadoc ("normalized in the one subtype routine `ModelContext.isSubtype`") → that method can never see a `PrecisionDecimal` | partial (V8, V14) |


---

## 2. SEVERITY SCALE, AS APPLIED

The scale is the brief's. Every row in §1 carries its justification as a trailing clause in the
claim ("*— S1 because …*") or, for the bucket rows, in the manifestation list. The distribution and
the exact rule used for the borderline calls:

| Sev | Definition | Count | Borderline rule actually applied |
|---|---|---:|---|
| **S1 CRITICAL** | silently wrong VALUES or TYPES delivered to a caller from ordinary, plausible source | 41 | A row is S1 if a **value or a Java carrier** that the caller receives is wrong — including "the right value under a type that cannot hold it" (D01, D03, D21) and "a row set that should not exist" (D23, D29-part, D79). A *missing* value under a `[1]` claim is S2, not S1 — unless Pure defines a non-null identity for that operation, in which case the delivered NULL is a wrong value (D27) or the convention is explicit in the same projection (D28). |
| **S2 HIGH** | a declared type/multiplicity the runtime violates, or an unchecked boundary that licenses it | 27 | Nullability and cardinality violations (D31, D32, D33, D37, D40, D47), type claims a value cannot satisfy (D34, D35, D42, D44), and the boundaries that *permit* an S1 elsewhere without themselves producing a value (D36, D38, D50, D81, D83, D85). |
| **S3 MEDIUM** | an internal exception (ICE) escaping on plausible user input, or a construct that type-checks and then cannot execute | 19 | The test is the *exception class*, not the phase: `LegendCompileException` subtypes and `NotImplementedException` are clean walls (the repo's own contract, `error/LegendCompileException.java:14-19`); a raw `IllegalStateException`/`IllegalArgumentException`/`NumberFormatException`/`IndexOutOfBoundsException`/`ClassCastException`/`ArithmeticException`/`StackOverflowError`/`SQLException` reaching the caller is S3. Bad SQL from a type-checked plan is S3 by the "cannot execute" arm. |
| **S4 LOW** | inconsistency, dead code, information loss with no observed wrong value | 12 + 1 META (D99) | Everything where an auditor explicitly could not produce a wrong value, plus every self-declared-latent item. Where a row bundles a latent item with a live one, the row takes the live severity and the latency is stated in the manifestation cell. |
| **S5 DOC** | a prose claim contradicted by the code | 5 | Ranked below every real defect, per the brief. Each S5 row points at the defect it is the documentation half of; where a doc lie has an independent observable consequence (D72's `format('%r')`) that consequence is named in the row. |

Three deliberate severity decisions worth flagging to the reader, because a different auditor could
reasonably disagree:

* **D31 is S2, not S1**, even though it is by volume the largest defect in the audit (960 of A20's
  1010 oracle violations, 12 distinct manufacturing sites, four orchestrator repros). Returning
  `null` where the type says a value exists is a multiplicity violation; it becomes S1 only where a
  correct non-null answer was available (D27) or where the same query demonstrates the intended
  convention (D28).
* **D81 is S2, not S5**, although it is a divergence from a specification rather than a runtime
  misbehaviour. It is scored as an unchecked boundary because five of its members
  (`percentile`/`corr`/`covarPopulation`/`covarSample`/`stdDev`) are the *direct cause* of an
  executed `null`-under-`[1]`, and 21 more widen the public language surface with functions that
  cannot be ported anywhere.
* **D55 (bad SQL) is S3, not S1**, despite `GROUP BY <constant>` being a latent silent-wrong-answer
  hazard: no auditor produced a query where the folded constant *was* a valid ordinal, so the
  observed behaviour is an error, not a wrong answer. If someone lands that repro, the row moves to
  S1.
* **D99 is marked S4 (META) rather than forced into the scale.** It produces no wrong value, so it
  is not S1–S3; it is not really an inconsistency or dead code either. It is scored low and flagged,
  because by the brief's rules it *is* low — while being, in my judgement, the most actionable row
  in the table: it is the reason 15 of the 19 checked findings could exist in a green build.

---

## 3. ROOT CAUSES

Fourteen design gaps generate the 105 defects. Each is stated as one sentence, with the defects it
explains. A defect can appear under more than one root cause; the **bold** ID is its primary home.

**RC-1 — There is no runtime enforcement of any declared multiplicity, and the compiler
manufactures `[1]` claims from data it has already proven wrong.**
The type system can express nullability and does compute it correctly at the store boundary
(`StoreCompiler.java:108-110, 170-171`), and then twelve separate sites overwrite that answer with an
unenforced `[1]` — `trustOne` as identity, LEFT-join schema copying, aggregate return declarations,
`toOne` deletion, match/cast promotion. Nothing downstream ever checks a cell against its bound.
Explains: **D31**, D32, D33, D37, D40, D47, D27, D28, D07(part), D80, D92, D93, D94, D97(part).
A36 measured its full extent: **117 of 117 executable `[1]` cells deliver `null`, including the
perfectly-matched diagonal** — an `Integer[1]` property over a plain `INTEGER` column that merely
lacks `NOT NULL`. The only thing that makes a `[1]` property non-null is declaring the physical
column `NOT NULL`, which the mapping layer neither requires nor checks. **`[1]` is an annotation,
not a guarantee** — and A27 shows the DDL generator drops `NOT NULL` and `PRIMARY KEY` anyway.

**RC-2 — The mapping is the bridge between the Pure type world and the physical schema, and it is
unvalidated: it relabels instead of checking.**
`coerceColumnToDeclared` is a coercion table with a fall-through, not a compatibility check, and
three sibling entry points implement it differently. Everything downstream (G's inference, I's MIR
typing, J's render, K's decode, the wire) then trusts a claim phase E never verified — and phase I
is explicitly told (`tolerateRead`) not to complain about it.
Explains: **D01**, D10, D17, D22, D31a/j/k, D68, D78, D91.
A36's 874-cell matrix is the exhaustive statement: 100 silently unsound, 12 ICE, 108 sound, and the
only loud arbiter is a *generic constructor check that the normalizer's own coercions are built to
satisfy*. The `###Relational` DSL accepts 21 column spellings and rejects 64 common SQL ones —
`BOOLEAN` among them (V22) — and has no `TIME` spelling at all, so `StrictTime` and `LatestDate`
cannot be mapped to any column type.

**RC-3 — `cast` is a value conversion, not an assertion, and `Any` is a universal solvent that
re-enters it.**
`CastPolicy` decides by primitive family and converts on a narrowing; `crossKindRaise` is disabled
whenever either side is not an SQL primitive, which is exactly the `Any` the Typer hands out at
three sites and the `Any[0..1]` a late-bound grid hands out for any name asked for. The composition
is a zero-enforcement re-labelling channel.
Explains: **D02**, D03, D31h, D34(part), D64(part), D85.

**RC-4 — The inference kernel has no way to *reject*: no bounded quantification, no occurs check,
no freshening, a one-level substituter, and three widening escape hatches.**
`Type.TypeVar` is `record TypeVar(String name)` — the model cannot express a bound at all — so
`unify(T, anything, new Bindings())` succeeds, conflicting bindings widen to `Number`/`Any` instead
of failing, and `Bindings` is keyed by the raw variable spelling. Every "check" that runs with a
fresh `Bindings` against a variable formal is therefore a no-op.
Explains: **D36**, D04, D29, D09(part), D26.

**RC-5 — Reference safety is applied to some type positions and not others, and the subtype relation
has a bottom-type arm inside its recursion.**
Property, parameter and return positions throw `Unknown type`; the `extends` list and a generic head
do not. The `Nil` arm answers `true` before checking that the right-hand side is even a real type.
Together, a one-character typo silently detaches a class from its hierarchy, and three lines of
ordinary Pure collapse the whole relation.
Explains: **D37**, D38, D17, D19, D76.

**RC-6 — β-substitution is not capture-avoiding, and constant folding is done with Java semantics on
Java carriers.**
Two independent substituters (`SourceSubst.inlineLets`, `UserCallInliner`) rename nothing in the
cases that matter, and `StaticFold`/`NormalizeFolds` use `Long.sum`, `Object.equals` and the static
multiplicity where the un-folded path uses SQL's widening, SQL's numeric comparison and a real
`CASE`. The same source therefore has two meanings depending on whether it was inlined.
Explains: **D05**, D06, D09.

**RC-7 — Precision and scale are a first-class part of the decimal type and are lost, guessed or
re-inferred at six different places.**
`decimalType` counts only the scale; the `PrecisionDecimal` algebra that would derive a result's
`(p,s)` has zero callers; `NameResolver` drops the declared `(p,s)` entirely; `SqlTyping` re-infers
the literal's type by significant digits where DuckDB sizes by rendered width; `CsvSeed` pins every
decimal to `DECIMAL(38,9)`; and the decode strips `(p,s)` from the SQL type name.
Explains: **D21**, D22, D42, D43, D44, D76, D78(part), D66(part).

**RC-8 — There is no single owner for any typing rule; the same question is answered by 2–6
independent hand-rolled implementations that disagree.**
Six spellings of "is this many?", six primitive-subtype ladders, three `__|__` implementations, two
LUBs, two column-identity rules, two Decimal-normalization conventions (nine sites carry the arm,
four forget it), three CSV parsers, six Pure→SQL type mappings. Each pair is a latent behaviour fork
and several are live wrong answers.
Explains: **D65**, D64, D12, D13, D27, D71, D72, D78, D79, D89, D90, D47.

**RC-9 — The native catalog is a hand-written port that was never diffed against the language it
ports, and the only guard compares it to itself.**
183 of 721 signatures diverge from real FINOS Legend; the golden test renders `Pure.all()` and
compares it to a file generated from `Pure.all()`, with a renderer blind to the two multiplicity
forms that matter. Several "compiler" defects are catalog defects: the `[1]`-on-empty aggregates,
the unbounded `abs<T>`, the invented `first(set,count)`, the `⊆T`-less `over`.
Explains: **D81**, D71, D82, D31d, D36(part), D07.

**RC-10 — The lowering, the dialect layer and the decode each re-decide types the frontend already
decided, and no backend-neutral contract holds them.**
`SqlTyping` re-infers decimal literals, `CastPolicy` runs a family lattice, `Lowerer` does model
lookups to pick MIR shape, the decode keys on the driver's Java class rather than the declared type,
and SQLite has no dialect object at all — it inherits identity `normalize`, ANSI type names and
**DuckDB's function spellings**.
Explains: **D15**, D16, D20, D25(part), D30, D41, D51(part), D60, D69, D86, D87.

**RC-11 — Phase H (store resolution) invents `ExprType`s rather than deriving them, and the
post-H root type is what every public surface reports.**
The resolver builds join schemas by copying the right side's columns verbatim, stamps the serialize
envelope with the getAll's type, re-types a scalar `map` as a relation, and deletes `toOne`. Because
`plan()`/`execute()` report the post-H root, the frontend's correct answer is unobservable.
Explains: **D45**, D31b/c/f, D46, D74(part).

**RC-12 — The instruments that would have caught most of this are structurally disabled.**
`SqlTypeCensus.MISMATCH` is unreachable because reconciliation runs first; `carryThrough`'s
`tolerated` tag permanently mutes the label/wire tripwire for a column and is stamped at every
declared/column kind mismatch; `normalizeMeta` has no arm for H2's own `DOUBLE PRECISION` so the
census is *also* wrong in the other direction; `executePlan`'s decline tunnel and `SeedableLets`
catch `RuntimeException` and re-run bare, which will swallow the egress's own soundness walls; and
`exec.Column.multiplicity` — the field a consumer would use to check a bound — has zero readers.
Explains: **D68**, D83, D89, D66, D31(why it survived), D01(why it survived), **D99** — and D99 is
the top of this chain: of 1 671 test methods that run a query end to end, exactly **one** relates a
declared column type to the delivered Java carrier. The suite tests compile-time typing densely and
row values densely, and the seam between them not at all.

**RC-13 — Error discipline is a frontend property only; it collapses monotonically down the
pipeline.**
`AGENTS.md` states every user-visible error carries a `LegendCompileException.Phase`. That holds at
25 % internal throws in the parser and 15 % in the Typer, and reaches **100 %** in `sql/`,
`sql/dialect/`, `exec/`, `plan/`, `values/` and `protocol/` — 850 of 1 168 throw sites overall.
`NotImplementedException`, the most-thrown type (293 sites), is a bare `RuntimeException`. A sweep of
247 queries over the whole native surface produced 85 internal exceptions against 5 user-facing
errors, every one of which had passed phase G.
Explains: **D96**, D50, D53, D54, D56, D58, D59, D60, D62, D63, D97, D98.

**RC-14 — Three synthesized namespaces inside the compiler are user-writable, with no reservation
and no check.**
`$` in a synth FQN is a legal identifier character (`Lexer.isIdentPart`), `u_map__` is a legal column
prefix that `ResultShape` keys on, `_i<N>` is the inliner's fresh-name space and is not reserved
against callee bodies, `pk_0`/driver-PK names are not reserved against user properties, and `__|__`
is a legal user column name. Each is a silent hijack or a collision.
Explains: **D19**, D26, D05(part), D98, D64(part).

---

## 4. PIPELINE-STAGE MAP

Each defect is assigned to the stage of its **root site** (not where it is observed). A defect whose
root spans stages is listed once, at the earliest stage that could have prevented it.

| Stage | Defects rooted here | Health verdict (grounded in the evidence) |
|---|---|---|
| **lex / parse** | D19, D54(part), D55(part), D50(part: `%25:00:00`, `1e309`, tvv overflow), D21(part: the D-suffix strip), D48(part: `tryTypedColSpec` swallow) | **Structurally sound, three sharp edges.** The literal carriers are correct at the node (`CInteger` holds a `Number`, `CDecimal` a `BigDecimal` — A22 §5), string escaping is exact over 11 forms (A05), and the multiplicity grammar round-trips on all 23 valid forms with clean `ParseException`s on 7 malformed ones (A02). The edges are: the `$` sigil is lexable so synth FQNs are forgeable (D19), no depth budget anywhere (D54), and three literal paths throw raw JDK exceptions instead of `ParseException` (D50). |
| **name resolution** | D76, D66(part), D82(part), D34-note (V34 quoted-FQN collision) | **The weakest link relative to its size.** `NameResolver` is a rebuild-if-changed walker whose convenience constructors silently drop `typeVariableValues`, `multiplicityArguments`, `multiplicityDeclared` and every `pos` — which is how a declared `Decimal(18,4)` becomes the uncompilable `Decimal<>` (D76) and how the duplicate-signature guard is defeated (D66). Nothing else in the pipeline is aware that the loss happened. |
| **normalize / mapping (E)** | **D01**, D10, D17(part), D22(part), D23, D31a/j/k, D70(part: poisons), D78, D79 | **The single largest source of unsoundness after the multiplicity family, and the one with the clearest fix.** There is no property-type/column-type compatibility check anywhere — only a coercion table with a fall-through — and three sibling entry points disagree about the same mismatch (A18 executed 36 cases: 11 unsound, 6 caught). The physically-correct multiplicity is computed one file away and deliberately overwritten. |
| **element compile (F)** | D17, D18, D37, D38, D43(part), D44, D50(part), D66(part), D81, D82, D94 | **Reference safety is real but has two named holes, and the catalog it validates against is 25 % wrong.** `ModelIntegrity` is loud and correct for property/parameter/return/association-end types, inheritance cycles and duplicate members (A03, A07 each falsified it against purpose-built models) — and it never looks at `extends` or at a generic head. `FunctionCompiler` lets a user model win against 413 of 431 natives. |
| **spec compile (G)** | **D31(the claims)**, D02(checker half), D03, D04, D08, D09, D25, D26, D29, D33, D34, D35, D36, D39, D40, D42, D44(part), D48, D52(part), D57, D58, D59, D85(part), D92(the declarations) | **The type checker's *structure* is good and its *rules* are not.** The dispatch is provably exhaustive (25/25 `ValueSpecification` variants, 51/51 `CoreFn` arms, all 33 checkers reachable, no `default`), unknown names are loud, duplicate colspecs are loud, and A22's 20-rule coherence probe found **zero** schema-arity/column-name/column-type/join-arity/lambda-param violations over 101 compiled queries. The defects are in individual rules — clamping, promotion, missing scalar-cell checks, upper-bound-only multiplicity checks — and in the kernel's inability to reject. |
| **inline (G½)** | **D05**, D06, D45(part: `fn::widen` G-vs-post-H), D83, D84 | **Unsound in the classical sense.** Two independent capture bugs, a fold that disagrees with the un-folded path in three ways, a swallowed `NotImplementedException` that misattributes every callee failure, and an effect memo that classifies a transitively effectful function as pure. α-equivalence — the most basic property a substituting compiler has — does not hold. |
| **store resolve (H)** | **D45**, D31b/c/f, D46, D67(part: walker holes), D74(part), D93, D97(part), D98 | **Invents types where it should derive them, and its output is what the public API reports.** H holds the correct answer in the same node tree it overwrites (A12's before/after dumps show `SAL:Float[0..1]` immediately under a `trustOne :: Float[1]`). Verified sound: the before/after root-type diff is identity on every non-defective shape, and column order is preserved exactly (A12 item 2). |
| **lower (I)** | **D07**, D02(policy half), D14(part), D25(part), D28, D30, D33(the disagreement), D51, D53, D55, D56, D64(part), D85, D86, D87, D91, D92(the emissions) | **The stated invariant "the Lowerer does no type inference" is false, and the advertised surface is larger than the lowerable one.** A10 classified every `instanceof Type.`/`Primitive.` hit in the package: model lookups (`classLayout.apply` ×5), family-lattice compatibility checks (`CastPolicy`), TYPE-identity folds and a SQL-layer re-inference of decimal literals. 112 of 523 planned overloads raise an internal exception. |
| **render (J)** | **D15**, D16(part), D55, D60, D71, D86, D89 | **Type names are near-uniform; behaviour is not.** Only 2 of 144 dialect type-name cells differ from the ANSI baseline (A11 §A) — and SQLite is handed DuckDB's *function* vocabulary with no `normalize`, so ~half an ordinary query surface fails loudly and one case (`toString()` on a temporal) fails silently as `null` under `[1]`. Set operations are rendered without parentheses (234 of 431 fuzz BAD_SQL hits). |
| **execute (K)** | D31(non-enforcement), D35, D52, D53, D74, D80, D84, D90, D96 | **Executes faithfully and checks almost nothing.** `Executor` has exactly one typed cell reader (`rs.getObject`) — the classic `wasNull` 0-for-null bug does not exist anywhere on the value path (A09 grepped the whole repo). But SCALAR checks only "zero rows", COLLECTION only the lower bound, TABULAR and GRAPH nothing at all; and the two broad catches in `StatementExecutor` will re-run a query bare after swallowing those walls. |
| **decode / wire back** | **D16**, D20, D30, D41(carriers), D45(part), D61, D62, D69, D77(part), D88 | **Backward is not the inverse of forward, and the backward direction is keyed on the driver's Java class rather than the declared type.** Six distinguishable Pure-type families collapse irreversibly (A08); one declared `Integer` reaches the caller as seven Java classes; a `DateTime` has four lexical forms across exits, two of which the platform's own reader rejects; and the DB-built JSON is not valid JSON for a non-finite Float or a >64-bit integer, which breaks the HTTP endpoint. |

| **plan text / lineage / validate** (a side-channel off H and I, not a pipeline stage — included because it is a public surface) | **D95**, D97, D89(part) | **Re-derives structure the typed HIR already holds, and gets it wrong.** Neither lineage surface uses the typed HIR; `PkInference` dispatches on a function's *simple name*; `ScanColumns` reports no lineage at all for a pivot; `ValidateDesugar` duplicates a diamond-inherited constraint and never checks its own `~message` column's type. Four `planToString` holes are internal errors on plain queries. |
| **the test suite** (cross-cutting) | **D99** | **Green, 4 278 tests, 0 failures — and blind to the seam.** 888 of 1 671 end-to-end test methods assert row values; 4 assert a column's declared type; **1** relates the two. Three findings are *pinned* by tests that assert the current wrong behaviour, so three of the fixes in this report will turn the suite red before they turn it green. |

**Cross-stage verdict.** The pipeline's *plumbing* is in good shape — sealing is exact (70/70
`TypedSpec`, 8/8 `com.legend.sql` roots), traversals that recurse through `children()` cannot skip a
variant, `children()`/`withChildren()` are mutual inverses on all 70 variants, phase ordering is
correct and the model is immutable after F. What is missing is *checking*: at every boundary the
question "does the value satisfy the claim?" is either not asked, asked with an instrument that has
been muted, or answered by a rule that has no single owner.

---

## 5. COVERAGE LEDGER

This section is what makes the audit auditable. Numbers are the auditors' own, as reported.

### 5a. Examined EXHAUSTIVELY (no sampling)

| Domain | Size | Result | Source |
|---|---:|---|---|
| Decimal `(p,s)` arithmetic — all ordered `(p₁,s₁)×(p₂,s₂)` pairs, p∈0..38, s∈0..p, per operator, vs an independently written Spark `DecimalPrecision` reference | **608 400** ×4 ops | 0 throws, 0 mismatches, 0 negative scales, 0 `scale>precision` | A01 |
| Multiplicity lattice — all pairs and all triples over the 20-element lattice (lower 0..4 × upper {0..4, ∞}) | 400 pairs, **8 000** triples | 0 commutativity/associativity/identity/monotonicity/hull failures; exactly 1 structural hole, at `[0..0]` | A02 |
| Subtype relation over 35 FQNs — reflexivity, transitivity, antisymmetry | 35, 1 225, **42 875** | 0 violations; `Nil <: X` holds for all 35 | A03 |
| `accepts`/`commonSupertype` over 42 constructible types | 42², 42³ | 19 antisymmetry violations (all generic-argument blindness), 6 commutativity violations (all `PrecisionDecimal`) | A03 |
| Native registry census — classes, enums, overloads, distinct FQNs, referenced type FQNs | 200 / 19 / **721** / 431 / 150 | 0 dangling FQNs, 0 duplicate signature keys, 0 unbound result type/mult vars, 3 phantom type params | A07 |
| Every one of the 721 overloads called with arguments synthesised from its own declared types, through G and through plan | **721** → 523 planned | G: 523 OK + 198 clean errors, **0 internal**; plan: 408 OK, 3 clean, **112 internal** | A06 |
| Overload-resolution ambiguity — 72-point argument grid per position, all (name, arity≤2) groups | **998 498** resolutions | 763 ambiguities, **every one** involving `Nil` | A07 |
| Catalog vs real FINOS Legend — all 721 signatures vs 24 172 real declarations from two on-disk checkouts | **721 vs 24 172** | EXACT 538 / RET_DIFF 16 / ARG_DIFF 131 / NO_SUCH_FUNCTION 36; 246 missing in-family, 1031 overall | A30 |
| Fuzz campaign over `Compiler.plan` + `Compiler.execute` against seeded DuckDB, 12 mutation-tested oracle checks | **18 000** generated + ~170 directed (12 800 in the clean campaign) | OK 9 020 (70.5 %), USER_ERR 2 170, **UNSOUND 1 010 (7.9 %)**, **BAD_SQL 431 (3.4 %)**, NOT_IMPL 168, ICE 1 | A20 |
| Dialect cast-type-name matrix — 18 `SqlType` rows × 8 dialect objects | **144** cells | 2 cells differ from ANSI; JSON/STRUCT gated; per-cell execution matrix run on 3 live drivers | A11 |
| Dialect capability matrix — 23 MIR hooks × 8 dialects | 184 cells | classified DC / ISE / UOE / OK; 8 throw sites proven reachable from a plausible query, 9 judged unreachable with reasons | A11 |
| `CastPolicy` primitive cast pairs | **144** (12×12) | every cell classified; the IDENTITY-lowering cells enumerated | A16 |
| Arithmetic result types — 9 numeric kinds × 9 × 6 operators against live DuckDB | **486** cells | 10 mismatches (all `REM` over decimals), 140 honest no-rule cells | A08 |
| Typed HIR — sealing, `children()`/`withChildren()` inverse, compact constructors, spec-carrying slots | 70 variants, **101** slots | 70 permits ↔ 70 files ↔ reflection; 0 type losses on a 36-variant round trip; 30/70 have a constructor, **0/70 validate `info`**; 12 slots missed by the walker, 7 real | A22, A12 |
| Typed-HIR coherence probe — 20 locally-derivable rules over a 114-line corpus | 112 queries / 101 compiled | **10 violations, all two known families**; 0 schema-arity / column-name / column-type / join-arity / rename-arity / concatenate / let-type / cast-target / lambda-param violations | A22 |
| MIR closure — reflection over `target/classes` | 8 sealed roots, 75 records, **190** components, 34 String-typed | all 8 sealed; every String component classified; 6 AGENTS.md counts wrong | A23 |
| Decimal-literal MIR fact vs DuckDB wire — intDigits × scale sweep | **287** rows + 41 leading-zero rows | 38 mismatches, all `intDigits==0`; 40 structurally illegal `SqlType.Decimal(s>p)` | A23 |
| Repo-wide fallback / defaulting / ICE census, then hand-review of **every** type-valued site | **4 237** rows | 10 violations, 24 suspicious-but-gated, 20 legitimate; 140 catch blocks classified | A21 |
| Decode table — 14 store column types × 3 backends, identical query and identical declared Pure types | 42 cells | **8 of 14 columns disagree** on Java class or value across backends | A09 |
| H2 date sweep across the Julian/Gregorian boundary | **33** dates | **16 BAD** (all < 1582-10-15) | A09 |
| `LiteralText.parse` spelling sweep | 37 spellings | every failure listed (non-finite, >long, `%latest`, `null`, `TDSNull`, …) | A09 |
| Aggregate matrix — 25 aggregates × 9 TDS column types; then every catalog aggregate over a zero-row relation | 225 + 32 | **18 declare `[1]` and return NULL**; 13 honest | A14 |
| `*Checker` reachability — every checker class vs `Typer.applyCore` | 33 / 33 | no orphan, no dead checker | A06 |
| Mapping mismatch matrix (property type × column type) | **36** of 286 possible pairs — **sampled**, see 5b | 11 unsound, 6 loud, 2 data loss, 2 carrier drift | A18 |
| Store→DDL type mappings censused against each other | **6** independent mappings | all six disagree pairwise; full diff tabulated | A27 |
| Protocol numeric-narrowing sweep | 17 sites | 3 defects, 12 sound, 2 noted-not-filed | A26 |
| Compile-seam agreement on root type | 10 probe queries (V9) + 18 000 fuzz queries (A20) | 0 divergences between `plan().rootType()` and `execute().returnType()` | V9, A20 |
| **Mapping type matrix — 23 Pure property types (incl. 10 `precisePrimitives` + an enum) × 2 multiplicities × 19 accepted column spellings, each run 3× (representative / edge / NULL)** | **874** cells | **100 silently unsound · 12 ICE · 108 sound · 628 rejected at compile · 26 loud at runtime**; **117 of 117 executable `[1]` cells deliver `null`, including the matched diagonal** | A36, V39 |
| `###Relational` accepted column datatype set, read from the parser then compiled | 21 accepted / **64** common SQL spellings rejected | no `TIME` spelling exists, so `StrictTime` and `LatestDate` cannot be mapped to any column; `BOOLEAN` is rejected | A36, V22 |
| **The JUnit suite, actually run** (in a separate copy of the repo, so the brief's `mvn` ban was not violated for other auditors) | **484 classes, 4 278 tests** | **0 failures, 0 errors, 16 skipped**; per-finding coverage over 19 findings: **0 COVERED · 3 PINNED · 15 UNCOVERED** | A37, V36 |
| Test-assertion census over every test method that executes a query end to end | **1 671** methods | 888 assert row values; **4** assert a column's `pureType()`/`multiplicity()`; **1** relates a declared type to a delivered carrier | A37, V36 |
| **Falsifier re-adjudication, two independent passes**, every repro re-derived from scratch with the falsifier's own fixtures (V2 additionally on 3 live drivers) | **202** rulings | V1: CONFIRMED **82** · OVERSTATED **5** · NOT-REPRODUCED **1** · NOT-REPRODUCED+MISATTRIBUTED **1** · FALSE-VERIFIED-SOUND **1** · **BY-DESIGN 0**. V2: CONFIRMED **103** · OVERSTATED **2** · DUPLICATE **6** · **NOT-REPRODUCED 0 · MISATTRIBUTED 0 · BY-DESIGN 0** | V1-falsifier, V2-falsifier |
| Public-API probe sweep (`server/QueryService`, HTTP endpoints, `ide/`, `cache/`, `spi/`) | **863** probes | **127 escape as raw internal exceptions** with no `Phase`; `Phase.RENDER` and `Phase.EXECUTE` have **zero** throw sites anywhere | A31 |
| `Primitive.DECIMAL` comparison-site census, every reference read with surrounding lines | **28** references | 14 carry the `PrecisionDecimal` arm, **7 do not**, 7 are non-comparison uses | V1-falsifier (supersedes V14's list of 4) |
| Throw-site discipline census over `core/src/main/java` | **1 168** `throw new` sites | 318 user-facing / **850 internal (72 %)**; 100 % internal in `sql/`, `sql/dialect/`, `exec/`, `plan/`, `values/`, `protocol/` | V37 |
| Native-surface execution sweep (whole `builtin/Pure` surface, post-G) | 247 queries | **85 internal exceptions vs 5 user-facing errors** | V37 |
| Type-decision-site census (the "NO FALLBACKS" invariant, measured rather than asserted) | **1 560** sites | 34 (2.2 %) can silently produce a type not derived from the model — 10 VIOLATION, 24 SUSPICIOUS; 24 proven reachable, 5 proven unsound. `default ->` 442, `default:` **0**; `FIXME`/`XXX` **0**; **none of the 140 catch blocks substitutes a `Type`** | A21, V37 |
| Scalar-function matrix — every scalar native's declared result vs its actual | full `builtin/Pure` scalar surface | **135 declared-`[1]` results return NULL**; `Integer[1]` decodes to 4 Java classes, one outside the 64-bit range | A24 |

### 5b. Examined by SAMPLING (auditor said so, and why)

* **Mapping type mismatches**: 36 of the 286 possible (13 Pure primitives × 22 `RelationalDataType`)
  pairs. A18 chose one per *kind pair* reachable in `coerceColumnToDeclared`'s decision table plus
  the boundary values the brief named. The 250 unrun pairs are the main residual risk in D01.
* **Overload ambiguity at arity ≥ 3**: 187 (name, arity) groups skipped — a 72-point grid at arity 3
  is 373 k resolutions per group. The *analytic* identical-parameter check does cover all arities
  and found only 3 benign ties, so the residual risk is a partial-score tie at arity ≥ 3 (A07).
* **The 87 "no scalar lowering registered" natives**: counted exhaustively, repro'd for a
  representative sample — they are one failure mode with one message shape (A06).
* **`CarrierStrategies` (1 264 lines)**: audited only where it bears on typing; the explode/collect
  rewrite family (~600 lines) was not exhaustively verified (A11).
* **`EngineStyleH2` (1 664 lines)**, **`Substitution` (3 167)**, **`GraphEmission` (3 429)**,
  **`AssociationJoins` (2 086, ~1 700 lines unread)**, **`CorrelatedSubselects` (2 390)**: read
  selectively at the sites that construct `ExprType`s or walls (A11, A12).
* **VERIFIED SOUND sections are auditor claims, not measurements, and one is provably false.**
  V1-falsifier ruled A10's "filtered-empty `->sum()` = NULL (`[0..1]`)" FALSE — it is `[1]`, i.e. the
  V19 defect recorded as sound. Wherever §5a cites a "0 violations" result it cites the probe that
  produced it; wherever this report leans on a prose "verified sound" claim it should be read as
  one auditor's assertion, at that auditor's confidence.
* **A20's run1 campaign (5 200 queries)** is excluded from the headline numbers: one DuckDB internal
  error invalidated the connection and the remaining 587 queries in that shard all failed with
  `database has been invalidated`. Those 587 are a harness artefact, not 587 defects; the auditor
  said so, added a reconnect, and re-ran as run3.

### 5c. NOT REACHED AT ALL — the honest gaps

**Not audited by anyone (still unassigned):** A31 public API (`server/QueryService`, `ide/`,
`cache/`, `spi/`); A33 `GraphEmission` deep (3 429 lines); A34 `Scalars` deep (3 495 lines);
A35 `EngineStyleH2` / `CarrierStrategies` / `EngineStyleDB2` deep. (A24, A28, A29, A32 landed
during synthesis and are folded in above.)

**Model shapes no auditor could construct:**
model-to-model (`PureInstanceSetImplementation`) mappings; XStore-over-Pure ends; JSON
`sourceUrl`/`ModelChainConnection` model connections; `Relation { ~func }` (RelationFunction)
mappings; aggregation-aware sets; `OtherwiseEmbedded`/`InlineEmbedded`. A12 expects D31a to
reproduce in the M2M lane (`ClassSources.java:183, :488` stamp row columns from the property
declaration with no physical cross-check) but did **not** run it.

**Milestoning / temporal**: exercised end-to-end by A15 and A12 only. A05, A18, A22 all declare it
unread. Bitemporal, `allVersionsInRange` (parser gap) and `TemporalFrame`'s 2 778 lines are unaudited.

**Constructs that are unimplemented, so their typing is untestable:**
class-rooted `pivot`, `asOfJoin`, `flatten`, `write`, `navigate`, `graphFetch` under H
(`NotImplementedException: … not resolvable yet (H2 vocabulary)`). A20's oracle therefore never
executed the `dynamicColumns` branch against real data.

**Backends and surfaces:**
`H2Modern` (no 2.3+ driver on the classpath); `EngineStyleDB2` / `EngineStyleComposite` against a
real DB2 (no driver — their `TypeNames.ANSI` mismatches are analysis, not measurement);
`Compiler.executeStreaming` beyond A19's single comparison; the HTTP server path beyond A09's
demonstration that the endpoint's own JSON reader throws.

**Types that are unreachable rather than sound:** `Byte`/`VARBINARY` round-trip; `StrictTime` in the
relation lane (the DDL parser has no `TIME` datatype); `TIMESTAMPTZ`/`OffsetDateTime` cells;
`java.sql.Struct` cells; `LatestDate` on SQLite.

**The test suite — this gap is now CLOSED.** The standing brief forbade `mvn`, so no *auditor* ran
the suite; A37 ran it in a separate copy of the repo and V36 verified the surefire reports. Result:
**4 278 tests, 0 failures, 16 skipped**, and of 19 findings put to a coverage check, **0 COVERED,
3 PINNED, 15 UNCOVERED** (D99). What remains unknown is coverage for the other ~80 defects in §1 —
A37 checked 19, chosen as the highest-severity items.

**Still not reached, and worth commissioning:** A31 (`server/QueryService`, `ide/`, `cache/`, `spi/`
— **the type surfaces actually exposed to callers**, and the only place a real consumer meets these
defects); A33 (`GraphEmission`, 3 429 lines); A34 (`Scalars`, 3 495 lines); A35 (`EngineStyleH2`
1 664 / `CarrierStrategies` 1 264 / `EngineStyleDB2`). A33 was commissioned and never appeared.

---

## 6. CONTRADICTIONS, AND WHAT THE EVIDENCE FAVOURS

Two falsifiers adjudicated **202 rulings** between them, each re-deriving every repro from scratch
with its own fixtures and reusing no auditor fixture file.

| | scope | rulings | CONFIRMED | OVERSTATED | NOT-REPRODUCED | MISATTRIBUTED | BY-DESIGN | other |
|---|---|---:|---:|---:|---:|---:|---:|---|
| **V1-falsifier** | `CONFIRMED.md` V1–V25 + CORRECTIONS; A01–A05, A07, A08, A10, A13, A14, A17 | 91 | 82 (24 dup-tagged) | 5 | 1 | 1 (with NOT-REPRODUCED) | **0** | 1 FALSE "VERIFIED SOUND" entry; 1 not adjudicated |
| **V2-falsifier** | A06, A09, A12, A15, A16, A18 in full; A20, A22–A24, A27–A30, A32 sampled; `CONFIRMED.md` V18/V20/V23/V24/V25. Re-run on DuckDB 1.5.0, SQLite 3.47.1 **and** H2 2.1.214 | 111 | 103 (6 dup-tagged) | 2 | **0** | **0** | **0** | 6 DUPLICATE |

**Zero findings were ruled BY-DESIGN by either falsifier.** Two were ruled NOT-REPRODUCED, and one
of those is a `CONFIRMED.md` *correction* that retracts a claim no auditor filed. Seven findings were
ruled CONFIRMED-BUT-OVERSTATED and are corrected in place in §1. One auditor's **VERIFIED SOUND**
entry was ruled false — which matters beyond that one row, because §5's coverage ledger is built
partly from VERIFIED SOUND sections.

Neither falsifier adjudicated A19, A21, A25, A26, A31, A33, A36, A37, the oracle-dependent bulk of
A30, or the non-sampled tails of A23/A24/A27–A29/A32. Rows resting only on those are listed in §6c.

### 6a. REJECTED / DOWNGRADED / CORRECTED

The three rows a reviewer should read first are the two **NOT-REPRODUCED** rulings and the one
**FALSE VERIFIED-SOUND** ruling; the rest are downgrades and precision corrections.

| Claim | Verdict | Reason, and what §1 now says |
|---|---|---|
| **`CONFIRMED.md` V24's repro and mechanism** — `\|[10]->map({x\| let y=$x; [7]->map(y\|$y)->toOne();})` ⇒ 7 shows let-inlining capture | **CONFIRMED-BUT-OVERSTATED — repro REJECTED, defect KEPT** | The falsifier showed three things: (a) the two programs are **not** α-equivalent — renaming the binder `z`→`y` puts a free occurrence of the let variable under a binder of the same name, which is exactly what α-conversion excludes; (b) under this compiler's own consistent lexical shadowing `7` is the **right** answer — `\|[10]->map({y\| [7]->map(y\|$y)->toOne();})` with no `let` at all also gives 7; (c) the mechanism sentence is **backwards** — the buggy SQL reads `y -> y`, so the substitution *did not fire*; had it fired and been captured it would read `y -> x`. **The underlying defect is real** (A06 #1 states it correctly). D05 now carries the falsifier's own isolating repro: `\|[10]->map({x\| let y=$x; [7]->map(x\|$y)->toOne();})` ⇒ 7 (correct 10), control binder `w` ⇒ 10. **V24 as published would be refuted by anyone who checked it and must not ship in this form.** |
| **`A09` F5's H2 half** — "`Float` → `BigDecimal` on H2" and the consequent "the same pivot compares unequal between backends" | **CONFIRMED-BUT-OVERSTATED — H2 half REJECTED** | Re-run on all three drivers with the falsifier's own fixture: H2 returned `java.lang.Double(1.5)`, identical to DuckDB. The **mechanism and the DuckDB half stand** (`Integer`-declared pivot column carries `BigInteger`; every pivot column's `multiplicity()` is `null`), and SQLite walls before the pivot (`DialectCapability: pivot reached a dialect without a PIVOT strategy`). D13 carries the DuckDB half only; the `PureAsserts.equalScalar` cross-backend-inequality consequence is dropped. |
| **`CONFIRMED.md` V14's parenthetical and CORRECTIONS §1** — "`InferenceKernel.java:576-580` DOES handle it — an earlier auditor's claim there is WRONG … filed by A01/A03" | **NOT-REPRODUCED — the correction corrects a claim nobody filed** | V1-falsifier grepped both files exhaustively for every `InferenceKernel` reference in the 550–600 range. The only hits are `A01:579` — which is inside the list headed *"Sites that **do** carry the extra `instanceof Type.PrecisionDecimal` arm"*, i.e. **A01 states the opposite of the claim it is accused of** — and A03's `conformsUpValueLattice:565`/`valueLub:554`, which are `LatestDate` and LUB claims, not `PrecisionDecimal` claims. The **code** fact is correct and V1 re-confirmed it by reading `isNumeric` at `:576-581` in full. **The retraction should be deleted from `CONFIRMED.md`: it defames a finding that is correct.** §1 carries the code fact only, with no attribution. |
| **"`PlanText.java:557` throws for any `DECIMAL` store column"** (filed by A01, forwarded to A29) | **REJECTED — MISATTRIBUTED** | A29 called it directly: `:557` **is** the `Primitive.DECIMAL` arm and returns `"Decimal"`; a real render of a `DECIMAL(10,2)` column gives `TDS[(price, Decimal, DECIMAL(10,2), "")]`. The real hole is `:567`, which has no `PrecisionDecimal`, `LatestDate` or `StrictTime` arm and is reached by a plan **let** (`let p = 1.5d`). D97 states the corrected form; A01's version is not carried. |
| **"`CDecimal`/`CFloat` are double-backed"** (hypothesis put to A26) | **REJECTED** | `CDecimal` is backed by `BigDecimal` and `CFloat`'s `double` correctly matches Pure `Float`. **No precision is lost at parse** — the loss in D21 is entirely at TYPE and at EXECUTE. Stated positively in §4's parse verdict. |
| **"the three compile seams disagree"** | **SPLIT, not refuted** | They agree on root TYPE (V9, and 0 divergences in 18 000 fuzz queries) and disagree on DIALECT SELECTION (V28) and on the **G-vs-post-H** type (D45). Reported as three separate facts; see §6b.1. |
| `A06` #9 preamble — "all four queries declare exactly `Integer[1]`" | **CORRECTED (nit)** | `max()` declares `Integer[0..1]` in the falsifier's run, and six queries are listed, not four. The substantive claim (`Integer`/`Long`/`BigInteger` under one Pure type) is fully confirmed. D41 now carries the nit. |
| `A06` header — "5 CoreFn parse names with NO Pure native" | **CORRECTED (nit)** | Only `new` has no native at all; `tds`, `typeAsDeclared`, `castAsDeclared`, `legacyNavigate` **are** registered under `meta::legend::lite::*` and are merely absent from `FN_BY_BARE`. The number is a measurement against that index, not an error, but it reads stronger than it is. |
| `A18` coercion matrix — `IntOnFloat` | **CORRECTED (nit)** | The carrier is `java.lang.Float(1.5)`, not `Double(1.5)`. Same category (a float carrier under `Integer[1]`); the class name in A18 is wrong. |
| `A16` title — "function bodies are **never** checked" | **CORRECTED — two defects conflated** | For a *monomorphic* function the declared return **is** checked, at G½. The genuinely unchecked case is the **generic** one (D04/V18), where no error ever fires and a `String` is delivered under `Integer[1]`. D04 now states both halves separately. |
| `A20` run1 — "587 BAD_SQL failures" | **REJECTED — HARNESS ARTEFACT** | One DuckDB assertion (`drop(0)` → `OFFSET 0`) invalidated the connection and the shard's remaining 587 queries all failed with `database has been invalidated`. The auditor identified this, added `ensureConn()`, re-ran as run3 and excluded run1 from the headline numbers. The underlying `OFFSET 0` defect is real (D55). |
| `A20` run1/pilot seeds "replay byte-identically" | **DOWNGRADED — stated caveat** | Seeds from bases 100 000–400 000 and the pilots predate a generator revision. Every A20 finding is restated as a literal hand-written query run through `Ev.java`, so no finding depends on a seed replaying. |
| `A11-F15` item 1 / `A23` — `CarrierStrategies.litText` names a pivot column with a Java `toString()` | **DOWNGRADED to CITATION + UNIT PROBE** | Proven by code reading and by a reflective call on the real method (`FloatLit[value=1.5, …]__\|__total`). Neither auditor landed an end-to-end Pure pivot repro. Kept in D13 on that basis, not as measured end-to-end output. |
| `A22`/`A15` — `TypedFrom` defaults an unspelled connection type to `"H2"` | **CONFIRMED (code), end-to-end UNPROVEN** | The falsifier read both sites in full (`TypedFrom.java:82-83` and the raw mirror at `:131-133`) and confirms both are unconditional non-enum → `"H2"`. A15 additionally ran `connectionNameIn` directly and got the wrong answer even when a type IS spelled as a String literal. No end-to-end wrong plan was produced. D47 says so. |
| `A12-H7` / `A22` — 7 `children()` walker holes | **DOWNGRADED to LATENT** | Proven at the `resolve()` API level (a live `TypedGetAll` survives in an `.orderKey` slot). Neither auditor could smuggle one through user-written query text: `orderKey` is only ever synthesised by the resolver, after the inliner. D67 states it as a provably incomplete invariant, not a live escape. |
| `A17-F15` — a resolved `[*]` column emits as `[0..1]` on the wire | **DOWNGRADED to LATENT** | Proven at the record level; no live call path emits post-`NameResolver` elements. (A26 independently confirms the loss itself is live in the *model* direction — that half is D76.) |
| `A04` — one-level substitution (`T:=X`, `X:=Integer`) | **DOWNGRADED to KERNEL-ONLY** | Proven at the kernel API; no surface query whose Phase-G root prints a raw `X` was found. Its *consequence* — the vacuous `unify` against a resolved bare variable — is proven end-to-end and is D04. |
| `A09-F18` — `Executor.pureOfSqlType` refuses `DOUBLE PRECISION` | **CONFIRMED at the switch, LATENT end-to-end** | The falsifier enumerated the exact accept/refuse lists over 34 spellings. Neither auditor reached the JDBC-metadata fallback after a pivot. Note this is the **same missing arm** A27 proves is live in `SqlTypeCensus.normalizeMeta` (D89) — fix them together. |
| `A14` — `Executor.java:742` positional-before-pivot precedence | **DOWNGRADED to UNPROVEN** | Reachable only with zero pivoted columns, where the positional read is correct. Not filed; not in §1. |
| `A16` — `IfChecker.failThenValue` | **DOWNGRADED to UNVERIFIED** | `meta::pure::functions::asserts::fail` would not resolve in the fixture. Not in §1. |
| `A21` V10 — `Lowerer.java:1414`'s else arm | **DOWNGRADED to UNREACHABLE-BUT-PRESENT** | No `computedColumns` call could be constructed whose `info.type()` is not relation-viewable. Only the `:1409` sibling is in D85. |
| `A23` — the `ADD_INTERVAL` StringLit channel is an injection vector | **DOWNGRADED — NOT an injection vector** | `DateShifts.intervalFn` throws on an unknown `DurationUnit`, so no user-controlled text reaches argument 0. D86 is scored as a structural hole plus 7 `ClassCastException` sites. |
| `A18` — `compileModel` defers mapping errors as "poisons" | **KEPT, flagged BY-DESIGN-BUT-STILL-A-DEFECT** | Documented as a deliberate adjudicated trade (`MappingNormalizer.java:326-338`). Kept in D70 because the *strict* entry point is the one that swallows and the error is re-reported later as a different failure — but the reader should know it is intentional. |
| `A22` — `TypedGraphTree.args` are deliberately not `children()` | **KEPT as DOCUMENTED DIVERGENCE** | A compensating path exists (`UserCallInliner.queryLets()`); it could not be exercised (no milestoned association in the fixture). In D67's manifestations, not scored on its own. |
| **`A10`'s VERIFIED SOUND entry** — "filtered-empty `->sum()` = NULL (`[0..1]`)" | **FALSE — a non-finding that launders a real defect** | It is `[1]`, not `[0..1]`: `fal::Item.all()->filter(p\|$p.qty > 200)->map(p\|$p.qty)->sum()` → `[G] Integer mult=[1]` → `[ROW] null`. This is exactly the V19/A02/A07/A14 defect (D31d), recorded by A10 as *sound*. **A "verified sound" claim that is false is worse than a missed finding**, and it is the one place where §5's coverage ledger inherited a wrong claim — corrected there. |
| **`A08`'s `rem()` over decimals — "[UNSOUND]"** | **DOWNGRADED — severity overstated** | The **Pure** declared type of the result column is `Number[1]` and a `java.lang.Double` *is* a `Number`, so nothing at the Pure type surface is violated. The real defect is narrower and internal: the SQL-MIR `TypeFact` says `DECIMAL(38,6)` where the wire is `DOUBLE`. Carried in D87 (MIR fact ≠ wire), **not** as a top-prize unsoundness. |
| **`A08`'s "`PrecisionDecimal(p>38)` produces SQL the backend rejects"** | **HALF DOWNGRADED — the reachable half only** | Half A confirmed: a `DECIMAL(50,10)` store declaration is trusted verbatim (`[G] Relation<(W:Decimal(50,10)[1])>`) and `AnsiSqlRenderer:924` renders it with no clamp. Half B was shown only by a **direct renderer call**; four Pure spellings that should force a cast on that column (`plus()`, `if()`, `toString()`, list literal) emitted none. Backend rejection *from user Pure text* is not established. |
| **`A14`'s repro fixture `T (GRP INTEGER, B BOOLEAN, …)`** | **CORRECTED — the fixture cannot have produced the pasted output** | The `###Relational` DSL has no `BOOLEAN` (`ParseException: unsupported column datatype: BOOLEAN`, `DatabaseProtocolParser.java:391`; the accepted spelling is `BIT`) — which is V22. V1-falsifier reproduced A14's finding with `BIT`, so the finding survives; the pasted fixture text does not. |
| **The "NO FALLBACKS. NO DEFAULTING." headline** | **MODERATED — see the fairness note** | Measured rather than asserted: of **1 560** type-decision sites, **34 (2.2 %)** can silently produce a type not derived from the model. `default ->` appears 442 times but `default:` **0**; `orElseThrow` 153 vs `orElseGet` 17; `FIXME`/`XXX` **0**; and **none of the 140 catch blocks substitutes a `Type`**. The invariant is ~97.8 % held at the site level. D74 must be read with that number attached: **the problem this audit found is not a codebase riddled with fallbacks — it is the absence of any check that a produced VALUE conforms to its declared type.** |

### 6b. Apparent contradictions between auditors — resolved

1. **"`plan()` and `execute()` agree on the root type" (V9; A20: 0 divergences in 18 000 queries)
   vs "they disagree" (A16 `fn::widen`; A12-H5 `serialize`).**
   *Resolved — different seams.* A20's oracle compares `plan().rootType()` to
   `execute().returnType()`; both are **post-H**. The divergences A16 and A12 report are between
   `Compiler.compileQuery` (**Phase G**) and the post-H surfaces, and the falsifier re-confirmed
   both (`\|fn::widen(1)` → `[G] Any[1]` vs `[PLAN-TYPE] INTEGER[1]`; `serialize` → `[G] STRING[1]`
   vs `[PLAN-TYPE] ClassType[biz::Emp] mult=[*]`). Both measurements are correct. The consequence is
   a **coverage gap, not a conflict**: no auditor's oracle compared G to post-H across a corpus,
   which is why D45 rests on hand-built cases. Recorded in §5c.

2. **"`->toOne()` over 0 rows raises correctly" (A02 VERIFIED SOUND) vs "`toOne()` is a no-op"
   (A20, A12-H1, V20, A24, A32).**
   *Resolved — three lanes, and the falsifier ran all three.* The **relation/collection** `toOne()`
   at a query root does emit `CheckedOne(CompactList(...))` and raises. The **`[0..1]`-stamped** arm
   falls to the default identity `return args.get(0)`. The **object-space** call is deleted outright.
   A24 sharpened it further: `toOne()` on a *column* does not assert (`SELECT t0.IN_V AS x` → `null`
   under `Integer[1]`) while the *literal* path does (`\|[]->toOne()` → `error(...)`). All auditors
   are right about the lane they tested. D31e/D31f name the broken lanes and D31 records the working
   one.

3. **"21 native signatures declare `[1]` and return nothing" (A07) vs "18 aggregates" (A14) vs
   "5 return-multiplicity narrowings" (A30) vs "135 declared-`[1]` results return NULL" (A24).**
   *Resolved — four counting bases, all correct.* A07 counts **registry signatures** shaped
   `[*] -> [1]` (21 signatures / 16 names). A14 counts aggregates **executed** over a zero-row
   relation that returned NULL (18 of 32 tried — a superset including `joinStrings`, `and`, `or`,
   `wavg`). A30 counts signatures whose return multiplicity is **strictly tighter than real
   Legend's** (5). A24 counts **scalar** results that returned NULL over the whole native surface
   (135). The sets overlap and answer different questions; D31d cites the first three, D92 the
   fourth.

4. **"`commonSupertype` has 0 non-upper-bounds over 441 pairs" (A03) vs "`LUB(Decimal,
   Decimal(10,2)) = Decimal(10,2)`, which is backwards" (A01).**
   *Resolved — different universes.* A03's 441-pair claim is over the **21 concrete** types, which
   excludes `PrecisionDecimal` pairs; A03 separately reports 6 commutativity violations over its
   42-type universe, **all** `PrecisionDecimal` — which is A01's finding. Both reports state their
   scoping.

5. **Three different root sites for the `Decimal → Double` carrier flip** (A10: `SqlTyping`;
   A01/A05/A17/A22: `Typer.decimalType`; A23: DuckDB's rendered width).
   *Resolved — all three, composed.* The Typer stamps a `(38,s)` the value cannot satisfy; the SQL
   layer independently re-infers `precision > 38 → DOUBLE`; and A23 showed the *real* switch is
   DuckDB's width `max(intDigits,1)+scale`, so a 38-digit **leading-zero** decimal also flips, and
   the intermediate `SqlType.Decimal(p,s)` is structurally illegal (`s > p`) for every `0.0…1`
   literal. D21 carries all three sites.

6. **A30's oracle vs the falsifier's inability to check it.** The falsifier confirmed the *runtime*
   half of A30's headline finding (`percentile`/`stdDev` declared `[1]` returning `null`) and
   explicitly declined the *upstream* half — "I have no independent legend-pure oracle either". A30
   does: two cloned repos at named commits, on disk, with a 24 368-declaration extractor. **Evidence
   favours A30**, and its provenance section is the reason. This is the one place in the audit where
   a load-bearing claim rests on a single auditor's checkout; the fix is cheap (re-run the extractor)
   and is the first thing a reviewer should redo.

7. **A02 "an association end `[2..1]` throws a raw `IllegalArgumentException`" vs A17-F12 "it is
   silently ACCEPTED".**
   *Resolved — complementary, at different phases.* V1-falsifier ran both: `compileModel` accepts it
   (`F12 assoc end [5..2] -> OK`), and the IAE fires only when a query navigates the end
   (`fal::P.all()->project(~[a:p\|$p.q.b])` → `IllegalArgumentException: upper (1) must be >= lower (2)`).
   Both auditors are right; D46 states both halves. This is the same phase-lateness pattern as the
   `DECIMAL(2,5)` correction above, and the two should be fixed together.

8. **"`exec.Column.multiplicity` has zero readers"** — A02, A09-F17, A25 and the falsifier all
   grepped independently (the falsifier over `core/src/main`, `core/src/test`, `pct/`, `nlq/`,
   `tools/`, `experiments/`). Four agreeing greps; no contradiction. Noted because it is the kind of
   absence claim worth quadruple-checking, and it was.

### 6c. UNRESOLVED

* **Rows resting only on un-adjudicated reports.** Neither falsifier touched A19, A21, A25, A26,
  A31, A33, A36 or A37, nor the oracle-dependent bulk of A30. That means **D14, D35, D54, D62, D69,
  D71(part), D76, D77, D80, D81(the upstream half), D83, D84, D85, D89, D99, D100, D102, D103,
  D104, D105** have not been through a from-scratch second pass. Of those, **D81 (25.4 % catalog
  divergence), D99 (the suite is blind) and D100 (one caller's data reaches another)** are the three
  most consequential single-sourced rows in the report, and are what a third falsifier should take
  first.
* **The two `A36`/`A37` headline numbers are single-sourced.** The 874-cell matrix and the
  4 278-test run are the two largest measurements in §5a and neither was re-derived. Both are cheap
  to re-run.
* **Is D31b (LEFT-join `[1]`) one defect or four?** A15 and A12 locate it at four construction sites
  that each copy `c.multiplicity()` verbatim; A20 treats it as one rule. Merged as one defect with
  four root sites because the fix is the same line at each. A reviewer wanting per-site tracking
  should split it.
* **The phase-lateness pattern.** V1-falsifier corrected two findings the same way — `DECIMAL(2,5)`
  and association-end `[5..2]` both pass `compileModel` and explode at Phase G on a *query*. How many
  other "Phase F rejects this" claims in §1 are really "Phase G rejects this when someone queries it"
  was not systematically checked, and it changes who sees the error and when.
* **Whether the `Nil`-extends and bad-`extends` holes (D37, D38) survive into H/I/J/K.** A03 proved
  Phase-G acceptance and stopped, because adding a store mapping for the exploit class would have
  changed what the repro demonstrates. Nobody ran it end to end.
* **Whether a null-outputs `SqlSelect` (D88) can be constructed in production.** A23 greps 0 sites;
  evidence of absence at one point in time, not a proof.
* **The blast radius of D81's 131 ARG_DIFF signatures.** All 131 are enumerated in A30's Appendix A;
  ~15 were analysed for consequence, ~116 were not probed for reachability.
* **Test coverage for the other ~80 defects.** A37 checked 19. The remaining rows' coverage status is
  unknown, and given the 1-in-1 671 assertion density the prior should be "uncovered".
* **The pinned tests.** Three fixes in this report (D02/V3, D20/V11, D43/V13) will turn a green suite
  red, because existing tests assert the current wrong behaviour — `EqualityWorldsConformanceTest`
  pins `1 == 1.0 → true` as a "declared divergence", and `PrecisionDecimalArithmeticTest` has nine
  tests including a 20×20×20×20 sweep over code whose only caller is that test file. Whether those
  pins are *decisions* to be revisited or *accidents* is a question for the repo owner, not for this
  audit.
